# Verification available now

PIO now has an M1 Cargo workspace, experimental fake-host process slice and pinned conformance pipeline. Its conformance-only Unix participant advertises Core/core-test and the five PLAN Execution features with public effects and bounded events, all through a labeled fake executor. Protocol v0.1.0 is released separately. Documentation checks validate structure only.

From this repository's root:

```sh
python3 scripts/check_docs.py
git diff --check
```

Python 3 standard library is sufficient; there is no package install step. The script checks required entrypoints, the local `CLAUDE.md` import, ordinary Markdown file targets, balanced fences and private machine paths in Markdown. It exits nonzero on an error. It reports cross-repository links it could not check.

When all five clones are siblings under one directory, also run from each repository root:

```sh
python3 scripts/check_docs.py --workspace ..
```

This additionally resolves Combraton GitHub main-file links against the sibling checkouts, including benchmarks. It does not prove those checkouts match the remote branches. Record their commits when using the result as integration evidence.

The GitHub Actions documentation job runs the first script on pushes and pull requests, with read-only contents permissions. It does not fetch sibling repositories. Remote URL reachability, Markdown fragment targets, Mermaid rendering, source-manifest consistency, semantic correctness, live harness instruction loading and product behavior need separate inspection. The script is intentionally small and is not a general Markdown parser.

## M1 build and conformance

The [journey-verification matrix](JOURNEYS.md) records the six required journeys and their shared evidence fields. All product journeys remain `not_evaluated` until their actual entry points exist and are exercised. A lower-level test cannot complete a TUI or real-adapter journey.

Prerequisites: Rustup, Python 3.12+, Git, GitHub CLI (`gh`), C compiler/linker and network access. An existing macOS GUI user domain or Linux systemd user manager is required for the packaging lifecycle test (CI checks the actual manager; no substitute process launch). Authenticate `gh` for release asset downloads (CI uses its read-only `GH_TOKEN`). The pinned toolchain is Rust 1.97.1; Cargo.lock is committed. From a clean clone on macOS arm64 or Linux x86_64:

```sh
rustup toolchain install 1.97.1 --profile minimal --component rustfmt --component clippy
rustc --version
cargo --version
cargo fmt --all -- --check
cargo build --workspace --locked
cargo test --workspace --locked
cargo clippy --workspace --all-targets --locked -- -D warnings
python3 scripts/check_docs.py
git diff --exit-code -- Cargo.lock
python3 scripts/fake_host_matrix.py --out target/fake-host --repetitions 3
python3 scripts/public_host_matrix.py --out target/public-host --repetitions 3
python3 scripts/client_recovery.py --out target/client-recovery
python3 scripts/codex_host_matrix.py --out target/codex-host --repetitions 3
python3 scripts/packaging_check.py --out target/packaging
python3 scripts/conformance.py --out target/conformance
```

These are the commands in [runtime CI](../.github/workflows/runtime.yml). CI uses macOS 15 arm64 and Ubuntu 24.04 x86_64 and asserts the architecture. It runs two jobs on each: `runtime` (build, tests, the matrices, conformance and packaging) and `lead-runs` (the L1, L1b and L3 rehearsals, their mutants and the L3 desk relay); each builds its own binaries and ends with its own leak check. These are tested CI environments, not a broader minimum-OS support claim. Use `target/debug/pio` explicitly: an unrelated historical `pio` may already be installed on PATH. The binary supports `--version`, `participant`, `conformance --data-dir DIR --config FILE --socket PATH`, and the experimental `fake` namespace. The runner creates the private socket directory, launch configuration and credentials; no user credential or configuration is used. The conformance service exits when its launcher closes stdin. It exposes core-test only through this test entrypoint.

The conformance command downloads the six pinned release assets into a fresh temporary directory, runs the existing integrity verifier, extracts the verified source archive, builds `combraton-conformance` with its released lockfile, runs runner self-tests and fixture validation, and runs all fixtures against PIO's descriptor. No sibling clone or pre-existing cache is required. It preserves the runner's exit status and unmodified manifest/transcripts; `pio-report.json` reports native outcome classes, counts for each fixture directory, and every unsupported/skipped coverage limit. Vendored validation schemas are compared byte for byte with the verified archive; `schema-verification.json` records the result. CI uploads the rendered descriptor, target, pin receipt, environment, manifest and transcripts even on failures. The [Execution checkpoint](work/m1/EXECUTION.md) records the latest per-directory results. The earlier [Core checkpoint](work/m1/CORE.md) remains historical evidence. A separate `pio-regressions/` manifest/transcript runs PIO supplemental fixtures against the same pinned runner and is never added to the released suite count. Protocol issue [#9](https://github.com/Combraton/protocol/issues/9) tracks the dependency-fixture coverage gap. Unsupported outcomes are never counted as passes. Cargo unit tests exercise admission/conflicts, transition validity, generation fencing, strict encoding, atomic transaction rollback, response loss and effect obligation storage. Public effect lifecycle claims are backed by the pinned Execution fixtures; unit tests alone would not establish them. Process behavior is exercised by the separate fake-host matrix command; its failures fail CI.

For offline asset reuse, append `--assets "$PIO_RELEASE_DIR"`; verification still runs. Choose a fresh output path for every run: existing evidence directories are never overwritten. Cargo downloads still need either network access or an existing registry cache. No real adapter, UI, memory-evaluation or end-to-end journey has passed.

For a change, report the command, exit status, environment, tested revision, real versus simulated dependencies, evidence location and untested limitations. Preserve the producer exit code when displaying shortened logs. Review the relevant diff against an explicit base/head.

## Standalone release evidence

The [Protocol pin procedure](work/standalone-0.1/PROTOCOL.md) uses Python 3.12+ (tested with 3.14.5) and downloaded release assets:

```sh
python3 scripts/verify_protocol_pin.py --assets "$PIO_RELEASE_DIR"
```

This checks pinned asset hashes, 539 bundle files and the released 420-file normative inventory. It does not build Protocol, run conformance or test PIO. A temporary modified-manifest negative control was refused with exit 1 for the expected checksum mismatch; see [pin evidence](work/standalone-0.1/evidence/pin-checks.json).

Follow [standalone release gates](https://github.com/Combraton/combraton/blob/main/docs/STANDALONE-RELEASES.md). Core no-optional-service tests, protocol conformance, real-adapter integration, comparative outcomes and UI usability are separate evidence classes. The [benchmarks repository](https://github.com/Combraton/benchmarks) owns cross-product scenarios/results, not this service's normative contract. No runtime benchmark has been implemented or run by the documentation setup.

## Experimental fake-host evidence

The corrected matrix runs eighteen cases three times (54 attempted runs): caller detach/daemon restart/reattach; duplicate and conflicting commands; SQLite write refusal before spawn; crashes after intent, host claim, release and receipt; lost host; stale controller; controller-generation rollback; lost-admission rollback within the same generation; the J3 marker-counter mutant, three replay-defense mutants, a wrong-reason classifier control, fenced release with known non-delivery, and a filesystem-level read-only store fault. Each case records its attempted repetition, observations and result. The mutant must fail the same `single_launch_identity_count` property used by the positive recovery case, specifically because two actual child starts were observed. A timeout, crashed test runner or unrelated assertion fails the matrix.

`matrix.json` includes source-file inventory and binary digests, platform, attempts and outcome counts. Per-case files preserve the journal/outbox, child-written append-only spawn/release markers, before/after identities and generations, and daemon logs. The journal-failure case also inspects the OS process table independently. CI uploads these beside the Protocol artifacts. Temporary stores are isolated under `/tmp` and retained for diagnosis; owned fake processes are killed only after comparing their kernel start identity. These tests use no real adapter, CBR, Combraton or user harness configuration.

To inspect the experimental path manually, use a new private directory and separate terminals:

```sh
mkdir -m 700 /tmp/pio-fake-example
target/debug/pio fake daemon /tmp/pio-fake-example
# In another terminal (each request connection closes after its response):
target/debug/pio fake request /tmp/pio-fake-example '{"op":"submit","id":"demo","payload":{"duration_ms":10000}}'
target/debug/pio fake request /tmp/pio-fake-example '{"op":"inspect","id":"demo"}'
```

This diagnostic JSON interface is not Core/Execution or an installable product CLI. Every execution is labeled `fake-host`. The child performs only bounded deterministic output and waiting. The journal-backed conformance participant separately implements Core/Execution and scripted discovery/workspaces/usage. Durable caller operations, content-addressed payloads and integration with the surviving-process host are exercised in the public checkpoint below. Neither the scripted observations nor the lower-level matrix alone establish the complete public durable-host path. Persistent launch guards and an external controller witness hold detected rollback; comprehensive backup/restore and storage-failure certification remain separate work. Do not remove guard/witness files to bypass a refusal.


## Public process and caller checkpoint

`target/debug/pio serve-fake --data-dir PRIVATE_DIR --config SERVICE_JSON --socket PRIVATE_DIR/public.sock` serves the public Unix binding independently of terminal stdin. Its explicit local configuration is `{"format":"pio-fake-service/1","protocol":{"format":"combraton-conformance-config/1","principal":"owner","credentials":[{"credential":"ccred1.owner.<43-character-test-token>"}],"executor":{"host_id":"durable-fake-host"}},"fake_host":{"duration_ms":10000,"fault":""}}`. Use an actual 43-character credential suffix. The matrix generates isolated configurations, credentials and stores; none are external provider credentials. `executor.script` is rejected in this mode. The conformance participant continues to use the separately labeled scripted test adapter.

The public process mode supports submit, inspect, reconcile, output, discovery and controller claims. Workspace/budget adapter behavior and cancellation are explicitly unavailable there; the five-feature runner claim belongs to the scripted participant, not a native adapter. The shared mutex service loop still serializes commands; durable child ownership is in detached `pio-host` processes. The process daemon also polls independently of connected clients, so detach does not stop observation.

`target/debug/pio client submit --store CALLER_DIR --socket PRIVATE_DIR/public.sock --credential-file CREDENTIAL_FILE --request ENVELOPE_JSON [--basis BASIS_JSON]` first commits the exact frozen command envelope and optional user-selected scope/policy basis. `target/debug/pio client reconcile --store CALLER_DIR --socket PRIVATE_DIR/public.sock --credential-file CREDENTIAL_FILE` reconciles pending identities after restart. An empty reconciliation stays pending; it does not create a new command. Use a separate private caller directory. This is a low-level attributed CLI, not the planned TUI or a completed product journey.

Public matrix artifacts include authenticated command/query transcripts, result-schema validation against unchanged vendored schemas, journal/outbox witnesses, child-created spawn/release markers and kernel start identities. The native schema cannot expose those detailed identities in `inspect`; they remain independent artifact witnesses. Each case records attempt/repetition and named property/refusal class. The diagnostic matrix remains separate test tooling.

`client_recovery.py` proves write-before-network on an absent endpoint, no spawn on empty reconciliation, recovery from a committed-but-lost response with one child, real fake-child output/exit, and digest-only output references in the journal. CAS object corruption is a Cargo test. These results do not qualify a real harness, native authentication or any end-to-end journey.


## M1 acceptance corrections

The public matrix now has 22 cases × 3 repetitions = 66 attempts. The diagnostic matrix remains 18 × 3 = 54. Public additions cover discovery, a crash between protocol dispatch-marker and host invocation-intent commits (exit 95), an actual admission-before-marker mutant and its wrong-reason control. A read-only journal oracle correlates execution ID to host command identity and compares the first dispatch-intent observation's sequence with invocation intent. Ordinary dispatch cases require exactly one pair. The cut records a marker but no host intent; restart conservatively records ambiguity and never spawns. Journal-sequence witnesses are preserved in `journal-order.json`.

Process-mode discovery reports the built-in labeled fake executable as detected, recognized, version-supported and reachable. Native authentication is **unknown**, so `usable` is false under the frozen all-positive rule. Protocol caller authentication is not harness authentication. Discovery itself spawns no child.

Restart records `execution.recovery.decided` followed by `execution.host.changed`, bound to the advanced controller generation. An existing child retains its original process/slot identity. Pending marker-present work becomes ambiguous until release/absence evidence reconciles it; an already proven release is preserved. The delivery evidence class **`child_release_marker`** means the identified fake child read its release message and wrote its marker. It produces the `delivered` determination, not native-provider acknowledgment or model comprehension. Recovered ambiguity resolves to `delivered` or `not_delivered` through reconciliation events with the matching frozen outcome. Recovery-state names are no longer used as delivery proof classes; the frozen optional `proof_class` enum is not extended or misused.

[Private packaging and user-job prototypes](../packaging/README.md) document layout, collision refusal, uninstall ownership checks, and the exact lifecycle command used in CI. `target/packaging` contains native-manager start/stop evidence and unchanged unrelated-`pio` witnesses.

## M2 capacity bound

ADR 002's [capacity bound](decisions/002-protocol-journal.md#capacity-bound-2026-09-16) adds `pio-core` and `pio-protocol` Cargo tests: 28 in total at `5be40f9`, and **31** after the admission headroom and proposal demonstration at `aa7ad78`. They cover: the exact byte and record limits and one over each; refusal before any staged row, using abort triggers; growth through unchanged records; accepted shrinking and no-op commits; subject, event and dedupe records counted by a real command; configured event retention changing the result; refused `execution.submit` creating nothing; the at-limit stall after a committed dispatch marker; queries and replays staying available during capacity-refused background commits, while other store failures stay `unavailable`; and equality with the canonical encoding length. An ignored probe measures commit cost at the record bound: `cargo test -p pio-core --locked [--release] -- --ignored --nocapture measure_commit_cost`. Its timings are observations, not a pass/fail property.

The headroom tests cover the exact admission thresholds for records and bytes (hard commits accept the same state), a submit refused one over the threshold while an ordinary put above it is accepted, and a submit admitted exactly at the threshold that runs to exit beyond it with no hard-limit refusal while a second submit is refused. `pinned_errors_cannot_distinguish_capacity_from_transient_commit_failure` checks the [Protocol #13](https://github.com/Combraton/protocol/issues/13) demonstration against the frozen error schema.

The public matrix adds `capacity_refusal_no_spawn` (23 cases × 3 = 69 attempts at `5be40f9`) and then `capacity_headroom_admitted_completes`, for **24 cases × 3 = 72 attempts**. The headroom case pads an execution-free store to 31,125 records. One durable submit must be admitted exactly at 31,130, which the case checks by counting the admission fact's new keys. A second submit must return `unavailable` with one `admission_projection_records` log line. The admitted child must be delivered with `child_release_marker` and exit 0 while the projection grows past 31,130, with no hard-limit refusal, one spawn marker and no process or execution record for the refused identity. It stops a durable service with no executions and uses the test-only `target/debug/pio fake fill-projection DIR RECORDS` tooling to pad the projection to 32,767 records through the same bounded commit. After restart, one public `execution.submit` must return `unavailable` / `same_command`. Since `aa7ad78` it is refused first by the admission threshold, so its log line names `admission_projection_records`. A public `execution.controller.claim`, which admits no work, must also return `unavailable`, with a second log line naming the hard `projection_records` limit. Journal, outbox, invocation rows and projection rows must be unchanged; spawn markers, process-table matches and an execution record for the refused identity must be absent; `execution.inspect` must answer `not_found`. The tooling then removes its filler and the same store and configuration must admit and spawn exactly one ordinary child, which checks that the refusal is not vacuous. The filler tooling is not a product command and never runs inside the service.

## M2 Codex qualification

`pio-codex` binds a user-selected Codex executable before any native work. `target/debug/pio codex qualify --executable PATH --work DIR [--expected IDENTITY]` resolves the npm wrapper the way the pinned wrapper does and records wrapper, Node (path, hash, version) and native binary hashes. It runs `--version` for the selected and native binaries, requires 0.157.0 (re-pinned from 0.155.1 on 2026-09-25), and only then generates the app-server JSON schemas. Each file's canonical parsed JSON digest is compared with the checked-in [schema identity](../adapters/codex/0.157.0/schema-identity.json) (314 files). The previous [0.146.0](../adapters/codex/0.146.0/schema-identity.json) and [0.155.1](../adapters/codex/0.155.1/schema-identity.json) identities are kept as the records each re-pin was measured against. Raw digests are provenance only. It exits 0 when qualified and 3 when refused; refusals are data (`unresolved_executable`, `version_unavailable`, `unsupported_version`, `native_version_mismatch`, `schema_generation_failed`, `schema_drift`). The executable always runs with an isolated `CODEX_HOME`. `pio codex config-snapshot --codex-home DIR` and `pio codex config-diff BEFORE AFTER [--fixture-root DIR]` record configuration digests and project trust entries, reporting paths only as digests plus a `fixture` / `outside_fixture_root` label.

Cargo tests (with labeled fake executables, so CI needs no Codex) cover canonical JSON, parsed-equal/raw-different acceptance, changed/added/removed schema refusal naming files, the isolated `CODEX_HOME`, an unsupported version never receiving Codex arguments, a missing executable refused as data, three npm wrapper layouts, the checked-in identity's own listing digest, and configuration trust-entry disclosure. They also cover the thread-settings guard, including a configured `approval_policy = "untrusted"` refused as unresolved rather than compared. `scripts/codex_host_matrix.py`'s file-change case proves only command approvals carry `kind`. Writing a fake executable and exec'ing one are serialized inside each test binary: a sibling test's fork inherits the still-open write descriptor and Linux then refuses the exec with `ETXTBSY`, which failed one Ubuntu CI run at `56dac9f`.

`python3 scripts/codex_offline_probe.py --executable PATH --out FRESH_DIR` needs an installed Codex and is not part of CI. It qualifies, then runs five cases, each with its own isolated `CODEX_HOME` and credential variables removed, sending only `initialize` and `thread/start` for a throwaway fixture repository and recording the configuration difference: the settings live runs request; absent settings on a fresh project; absent settings with the project already trusted, which is what the thread-settings guard assumes; `approvalPolicy: "untrusted"` requested per thread, which R5 and R6 need; and `approval_policy = "untrusted"` configured, which 0.155.1 refuses to start with. Workstation evidence and findings are in [codex-qualification](work/m2/codex-qualification/README.md). None of this is a live run, real journey or model-backed result.

## M2 Codex adapter offline matrix

`target/debug/pio serve-codex --data-dir DIR --config FILE --socket PATH` serves the public Unix API with the ADR 003 Codex adapter. A `pio-codex-service/1` configuration names the selected executable, its explicit environment, the Codex home, the fixture root, and optional thread settings (`danger-full-access` and approval policy `never` are refused). For a real executable, startup qualifies it and refuses with `codex_not_qualified` before any native work. `labeled_fake: true` runs the labeled test double `pio codex fake-app-server` instead; its evidence is labeled `pio-fake-app-server`, and discovery reports it as not Codex and never usable.

`python3 scripts/codex_host_matrix.py --out target/codex-host --repetitions 3` runs 41 cases × 3 (123 attempts) against the fake app-server, with independent markers written by the fake, read-only journal and host events, and the process table:

- a J1-shaped turn: `provider_ack_id` delivery from the `turn/start` response, spooled output, observed token usage, exit 0, fixture trust-entry disclosure, and brief bytes kept out of the journal; and `config_after` written **before** `app_server_exited`, the event that makes the view `exited`, as the Claude and OpenCode hosts write theirs (review 47: the other order raised `IndexError` whenever a reader won the race);
- approval decline and accept delivered natively, with a repeat answer `not_found`;
- interrupt observed as `cancelled`;
- steering acknowledged with behavior `not_observed`;
- the suppressed-acknowledgment control never claiming `acknowledged`;
- a missing brief, a fixture outside the root, and a content digest mismatch refused before any app-server starts;
- the `kind` 0.155.1 added to command approvals recorded with the action: `command` on the accepted case, `writeStdin` on the declined one, and `command` again where the optional field is absent;
- an unqualified executable refused at service start;
- a permission-grant request refused natively, with no action surfaced and nothing answered, failing on the named assertion `permission_grant_surfaced_as_action` rather than on a timeout if one ever is;
- daemon restart with the same host and app-server, one turn and an advanced generation;
- a lost host after acknowledgment reported `unknown` with no respawn;
- discovery reporting authentication only after a launch observed it;
- widening approval decisions (`acceptForSession`, execpolicy and network-policy amendments) refused as `invalid_envelope`, with no control or native answer;
- a 3-second execution deadline stopping the turn with a real `turn/interrupt`, recorded `deadline_stop` outcome `interrupted` and a clean app-server exit;
- a configured `read-only` sandbox making a `workspace-write` request refuse before any app-server starts.
- `approvalsReviewer` never sent, and the harness's answer asserted `user` on every run: a `guardian_subagent` answer and an **absent** one both end the run `approvals_reviewer_not_user` before any turn starts, because 0.155.1's `ThreadStartResponse` makes the field required and silence is not `user`.
- For L3 (owner decisions of 2026-09-25), seven more:
  - **The lead tool is on the lead's thread only.** A witness MCP server named in `thread/start`'s own `config.mcp_servers` records `initialize`, `notifications/initialized`, `tools/list` and both `tools/call`s. `mcp_servers_sent` names it for that run and names nothing for a second run without it. Its `pre_allowed_tools` (`start_run`, `read_run`) mean Codex asks nothing.
  - **An MCP tool-call approval comes to the caller.** It surfaces as an action with `approval_kind: mcp_tool_call`, the server, and what Codex offered to remember (`persist`). `accept` is sent as `{"action":"accept","content":{}}` with no `persist`, and `decline` as `{"action":"decline"}`. Only the accepted call reaches the server.
  - **An MCP approval nobody answers lapses.** After the caller's delivery timeout, PIO sends one decline, recorded `request_denied_by_default`, and the tool never runs.
  - **An elicitation that is not a tool-call approval** (a URL-mode login) is still declined natively, with no action.
  - **Model and provider come from Codex's own answer before the first turn.** With `gpt-5.6-terra` asked for on `openai`, `model_checked` holds before `turn_start_sent`. A different model, or a different provider, ends the run `thread_model_mismatch` with no turn sent.
  - When Codex asks before an MCP tool call, and how it reads an answer, is **read from its source** at `rust-v0.157.0`, not measured: the fake mirrors it (`crates/pio-codex/src/fake_turn.rs`).
- After the review of L3, two more cases, and assertions on the wire:
  - **A command approval nobody answers lapses** (`command_approval_lapses`): one decline of PIO's, received once by the fake, the command never runs, and the stream's decision is PIO's, basis `deadline_lapsed`.
  - **A form asking for data**, in form mode but without `_meta.codex_approval_kind`, is declined natively and never surfaced (`form_elicitation_declined`).
  - The lead-tool cases read what went on the wire: the host's read-back of its own `thread/start` request (`mcp_servers_sent.servers`) and the fake's `thread_config_received` marker both hold exactly `start_run` and `read_run` at `approve` and no server-wide default. The fake records every answer it receives (`response_received`, and `second_response` for a request answered twice): the accept carries no `persist`, and every request in the approval and lapse cases is answered once.
  - A native decline records what was asked (method; for an elicitation the server, mode and Codex's approval kind; for a permission grant the kinds asked for, only those that are not null; for a request for the user's input how many questions and whether it is an MCP tool-call approval) and the true reason, never a URL, form, path or question's text, and every run's `execution.exit.observed` carries the list under `pio.combraton.dev/native-declines`. The fake's permission grant asks in 0.157.0's shape (`fileSystem.write` under the cwd, `network` null), so the kinds recorded are `['fileSystem']`.
  - A command approval carries its placement (the command's working directory classified `inside_fixture` or `outside_fixture` against the workspace, as a label and a digest), a network flag, and Codex's reason, in the action and in the approval payload. The fake's reason is null unless a case sets one, as in both measured approvals (M2 R5, R6).
- After the second review of L3, five more:
  - **A working directory through a symlink** (`approval_cwd_through_a_link`): a command approval whose cwd lies under the fixture by its spelling but reaches outside it through a link is placed `outside_fixture`. The path walk in `pio-claude`, which the Codex host shares, followed a link before the last component out of order until this round, so such a path could be placed inside; the defect dates from M3 and affected Claude placement too. `crates/pio-claude/src/tests.rs` covers `inside/../../outside`, a link to `.` followed by one out, a link out of a subdirectory, and the label of a directory inside.
  - **A working directory whose resolution passes `PATH_MAX`** (`approval_cwd_past_path_max`, round 3, R3-HC-1): two links, each shorter than `PATH_MAX` and together longer, the second inside the directory the first leads to, and then a link out. The kernel follows it one component at a time; `readlink` refuses the whole resolved path (`ENAMETOOLONG`). The resolver used to take any `readlink` error for "not a link" and read this `inside_fixture`; it now fails closed, so the placement is `not_classifiable` and the relay leaves it to the owner. `crates/pio-claude/src/tests.rs` covers the same path, a loop, a chain of six links ending outside, and a chain of 41 (past the resolver's budget of 40), each unresolvable except the six; and, after round 4 (R4-HC-3), a link out held in a directory this user cannot search (`EACCES`) and a path through a file (`ENOTDIR`), each unresolvable, so a resolver that took either answer for "not a link" fails its test.
  - **A sub-agent's thread is not the run's** (`subagent_thread_attributed`, `subagent_interrupted_with_the_run`; round 3, SPEND-2). Codex 0.157.0 has `multi_agent` on by default and attaches every thread it creates to every initialized connection, so a sub-agent a run spawns reports on the run's own connection. The fake spawns one as 0.157.0 does (read from source, not measured: the parent's `subAgentActivity` item, then the agent's own thread's turn, usage, words and a command approval). The host attributes every notification by `threadId`: the agent's `turn/completed` does not end the run's turn, its words are not the run's output, its request is declined by PIO with its thread named and never put to the caller, and its usage is kept apart and added to the run's, which is the sum over the run's threads (the stream's usage observation is 5,000 + 5,000 + 42 = 10,042). The run's exit carries `pio.combraton.dev/other-threads`: each thread, how it appeared (named by the run's own `subAgentActivity` item, before it said anything) and its usage. A cancel interrupts the agent's running turn with the run's, and a run whose own turn ends first interrupts any agent still running and waits up to ten seconds for its last report. Host mutants killed by these cases: the run total as the largest thread's rather than the sum; every thread taken for the run's; other threads not interrupted. Round 4 (R4-HC-1): the agent's interrupt must carry the cancel's own control id and come before the run's own turn ends, so a host that interrupted it only in the sweep after the run's turn (control `run-ended`) fails `subagent_interrupted_with_the_run`; and what the host has interrupted is a thread's turn, not the thread, so an agent set going again on its thread after the cancel (`subagent_turn_after_interrupt`, the fake's `subagent_restarts`) has its new turn interrupted by that sweep, which a host keyed by thread alone skips.
  - **Codex's unmetered features off per launch** (`features_off_decision_sent`, round 3, SPEND-2; round 4, U1): a `codex.features_off_decision` in the service configuration puts every key `pio_codex::features_off` lists in every thread's `thread/start` config: `agents.enabled`, `features.multi_agent` and `features.multi_agent_v2` false (sub-agents), `features.memories` and its legacy alias `features.memory_tool` false (memories), `features.goals` false, `web_search = "disabled"` (standalone web search) and `features.image_generation` false. Read back from the request, witnessed by the fake, and the fake then spawns no agent and runs no memory pipeline. The keys and their sources at rust-v0.157.0 are in `crates/pio-codex/src/lib.rs`: `agents.enabled = false` turns multi-agent off whatever the model's catalog says, while `features.multi_agent = false` alone does not for `gpt-5.6-terra`, whose bundled entry says `v2`; `memory_tool` sorts after `memories` and would decide it; `web_search` decides the mode before any feature does. The setting is accepted only as a recorded owner decision (`FEATURES_OFF_DECISIONS` in `crates/pio-protocol/src/stream.rs`, which holds one: the owner's of 2026-09-26, `owner-2026-09-26-l3-codex-unmetered-features-off`), or as the rehearsal's own token beside a labeled fake; a Cargo test covers the refusals (the rehearsal token and any unrecorded decision beside a real Codex), that the recorded one passes that check, and that the old agents-only setting is refused as unknown. A host that sent only the three sub-agent keys fails this case.
  - **A turn Codex starts by itself after the run's** (`continuation_interrupted`, round 4, SPEND-9). Codex 0.157.0 has goals on by default, and an active goal starts a continuation turn on the same thread right after `turn/completed` (read from source, not measured). The host used to stop reading the run's own thread at its first `turn/completed`, so such a turn ran unseen and uncharged. It now keeps reading that thread for three seconds (`CONTINUATION_GRACE`, shorter than the twenty-second stop deadline): a `turn/started` there is recorded (`continuation_started`), interrupted under control `continuation`, and waited for; its usage counts with the run's; a request there is declined by PIO; and the exit carries the list (`pio.combraton.dev/continuations`) while the run's own turn status stands. The fake plays a continuation (`continue_after_turn`) unless the thread's config turned goals off, which `features_off_decision_sent` checks. A host with no grace period fails this case.
  - **File changes with no root, and with a root inside** (`file_change_no_root`, `file_change_root_inside`; round 3, R3-HC-6): the first says `grant_root_requested: false` and carries no placement; the second places its root `inside_fixture` as `<fixture>/src`, by label and digest; the relay leaves both to the owner. A host that reported every file change as asking for a root fails the first.
  - The fakes' `item/tool/requestUserInput` carries `isBlocking: true`, required at 0.157.0, and a sentinel in its header and option label as well as its question and description; `user_input_declined` finds none of them in what PIO recorded or carried, and a host that copied headers and labels fails it (R3-HC-5, R3-HC-7). The lead's `!shell` item names the thread's cwd, a string at 0.157.0.
  - **A request in every wait before the first turn** (`early_declines_in_every_wait`, `early_decline_then_exit`; round 3, R3-HC-3 and C3-2). The fake sends a request before it answers `initialize`, `account/read` and `thread/start`: each is declined once, recorded with `phase: before_turn` and which `wait`, and carried on the exit. And one during `account/read` after which the app-server exits without answering: the host's wait fails, and the decline it sent is still recorded, because each is recorded as it is sent rather than returned with a successful response. A host that answered the `account/read` requests without recording them fails both. When the start fails, the run has no `execution.exit.observed`, so the list rides on the `execution.runtime.changed` that marks it `refused_before_delivery`, under the same key, and `early_decline_then_exit` reads it there (round 4, R4-HC-2: before this it was kept in the host's events file alone). Only the `thread/start` window is a shape Codex 0.157.0 sends (an elicitation comes from a thread, and carries its id): there the fake now sends the new thread's own id, and the case checks it from the fake's own record; the `initialize` and `account/read` requests are labeled defensive, sent with no thread id for want of a thread (round 4, R4-HC-5). The fake's network ask now names no command, cwd or command actions, as 0.157.0's network presentation sends none, and `network_and_unplaced_approval` checks the host recorded none.
  - **A request before the first turn** (`early_elicitation_declined`): an elicitation sent while the host waits for `thread/start` is answered at once with PIO's decline (received once by the fake), recorded with `phase: before_turn`, carried on the exit, and the turn still runs. Before this round, a request during the `initialize`, `account/read` or `thread/start` waits was dropped unanswered.
  - **A stream Codex is retrying** (`stream_retries_recorded`, round 4, SPEND-10): Codex's `error` notification with `willRetry: true` is kept with its flag and its turn, and is not the end of the turn, so the L3 runner can count each retry per run in its receipt. A host that dropped `willRetry` fails this case.
  - **What makes a request an MCP tool-call approval** (round 4, R4-HC-4). A question of `item/tool/requestUserInput` is one by its id alone (`mcp_tool_call_approval…`, `core/src/mcp_tool_call.rs:1443` at rust-v0.157.0), never by its words, which name an app, a connector or a server: a unit test in `crates/pio-host/src/codex.rs` pins both ways (Codex's id in words naming no server; a model's question naming an MCP server with no such id). And only a form elicitation is one: a url-mode elicitation whose server-written `_meta` claims `codex_approval_kind: mcp_tool_call` is declined natively and never surfaced (`url_elicitation_with_approval_kind_declined`, and a unit test). A host that decided by the question's words, or dropped the form check, fails them.
  - **A request for the user's input** (`user_input_declined`): `item/tool/requestUserInput`, Codex's route for an MCP tool-call approval when elicitation is off, is declined natively and never surfaced. The record counts the questions and says it is a tool-call approval; a sentinel in the question and in `_meta` appears nowhere in the stream.
  - **A command approval asking for the network, with no cwd** (`network_and_unplaced_approval`): surfaced with `network_approval: true` and placement `not_classifiable`, and the L3 relay answers none of the three variants (network, unplaced, both), only the same command inside the fixture with no network ask.
  - **A file change asking for a root** (`file_change_grant_root`): `grant_root_requested: true`, and the root classified by label and digest (`grant_root`, `outside_fixture`); the L3 relay leaves it to the owner.

A `harness_or_assertion_failure` in this matrix records `failed_at`, the file, line and source of the frame that raised, beside the exception.

Every matrix submit carries the live timeouts (`delivery` 120 s, `execution_deadline` 600 s) unless a case tests the deadline.

The matrix proves adapter plumbing only. It runs no real Codex, uses no model and establishes no journey.

## M2 live Codex runs

`python3 scripts/codex_live_run.py --run R1|…|R6|model-list|discovery|wrong-executable` drives the user's installed Codex through `pio serve-codex`. It is **not part of CI**, needs an authenticated Codex, and spends real tokens for R1 to R6. Each run uses its own private store and a throwaway fixture repository under `$HOME/pio-m2-live/fixtures`; raw transcripts, task output, configuration copies and the usage ledger stay under `$HOME/pio-m2-live/private` at mode 0700. The public receipt in [codex-live](work/m2/codex-live/) holds digests, identities and observed facts only, with `$HOME` and credentials redacted.

Stops are the **runner's** rules, not the product's: PIO enforces no budget, and every stop below is `scripts/codex_live_run.py` watching observed usage and calling `execution.cancel`. No run starts once cumulative observed usage reaches 800,000 tokens, which is 80% of the owner's 1,000,000 Codex cap; a run whose observed usage passes its own limit is interrupted with `execution.cancel`; a run that ends without a usage report stops the sequence. R1 carries a 50,000 limit and R2 to R6 carry 250,000. A limit can only be enforced when the harness reports usage, so a run stops at the first report above it, not at the limit itself.

`model-list`, `discovery` and `wrong-executable` start no turn and cost nothing. `discovery` queries `execution.discovery.list` twice, against a fresh store and against the store a completed run left behind, to show authentication moving from `unknown` to observed without any app-server starting.

Results and the journey marks they support are in the [M2 acceptance packet](work/m2/ACCEPTANCE.md).

## M3b OpenCode qualification

`pio-opencode` binds the user-selected OpenCode executable before any native work. `target/debug/pio opencode qualify --executable PATH --work DIR [--expected SURFACE]` resolves and hashes the binary, requires version 2.0.11 (**re-pinned from 2.0.1 on 2026-09-21** after npm self-updated the install; only the top-level help moved), and digests the command-line surface — the top-level help plus six subcommand helps — against the checked-in [surface identity](../adapters/opencode/2.0.1/surface-identity.json). Exit 0 qualified, 3 refused; refusals are data. Measured: the installed 2.0.11 qualifies with zero drift, and pointing it at Claude Code refuses `version_unavailable` without running a single OpenCode-specific argument. The re-pin is itself evidence the check works: 2.0.11 was refused as `unsupported_version` on the morning of the first live run, before a token was spent.

`target/debug/pio opencode service-admit --config FILE --work DIR` makes the pre-session decisions: the settings allowlist, an environment that must carry no credential variable and nothing outside `PATH`, `HOME`, `USER`, `OPENCODE_CONFIG_DIR`, the dated model exception, and the **owner's exclusion of the Juspay Grid provider**, which is refused outright rather than left unevaluated. The record carries `session_started: false` and the owner's own background service by digest, so a run can prove it did not move.

`session_configuration_guard` is the owner's rule of 2026-09-20: **refuse unless the session's reported provider and model equal the requested ones.** It exists because a missing route does not refuse — measured, it silently substitutes a free built-in model. Unlike Claude Code's `system/init`, ACP reports the session configuration **before any prompt**, so this refusal precedes delivery, and the guard records `checked_before_delivery: true` to keep the two from being read as the same guarantee.

Tests cover an unsupported version refused before any further argument, surface drift naming the command that moved, a session reporting the requested model, a silent downgrade to another provider, a different model from the right provider, a session reporting nothing at all, the excluded provider, a model without the dated exception and a run without a model, and a credential variable in the environment.

`target/debug/pio opencode fake-acp` is a **labeled fake OpenCode ACP server**. It speaks the shapes measured from the installed OpenCode at zero tokens — the `initialize` result, and `session/new` returning `configOptions` with the model the session will use — plus the `session/request_permission` exchange, which comes from the **ACP specification and is not measured against 2.0.1**; ADR 005 records it as unverified and PIO forwards no decision whose single-use form it has not measured. It runs no model, tool or command and labels everything `pio-fake-opencode-acp`. It records any forbidden flag it is passed, and any field on a permission outcome that would make the decision outlive the request.

`python3 scripts/opencode_host_matrix.py --out DIR --repetitions 3` is the **offline OpenCode matrix**, run in CI. Twenty-five cases, each three times, against that fake, and **never anywhere near the owner's running service**: the fake is a child of the matrix on stdio, and every case records the owner's `serve --service` process before and after and fails if it moved.

**The permission shape is now measured.** R5 produced the first real `session/request_permission` from OpenCode, offering `once` / `always` / `reject` with kinds `allow_once` / `allow_always` / `reject_once`. The fake's invented ids had included `allow`, which **is not an id this harness uses** — a host hard-coding it would have selected nothing on a caller's allow, while a hard-coded `reject` would have worked by coincidence. The fake keeps deliberately different ids (`opt_1`, `opt_2`, `opt_3`) so that any hard-coded id fails offline. What the offline cases still cannot show is *when* this harness asks: measured, it asks about a read outside the workspace and does not ask about a shell command inside it.

The case that carries the owner's rule is `silent_downgrade_refused_before_any_prompt`. The session reports `opencode/nemotron-3.5-lightning-free` while the run requested a MiniMax model; the guard refuses on both provider and model, and the proof is that **no prompt is ever sent** — `session_created` appears in the fake's markers and `prompt_received` does not. The others cover the requested model allowed, a wrong model from the right provider, a session reporting nothing, the owner-excluded provider, a model without the dated exception, three credential variables, an unqualified executable, the forbidden flags with a control that proves the detector works, and an always-allow option offered and never taken.

`unqualified_executable_refused` caught a real defect on its first run: `service_admission` computed a qualification verdict and admitted the configuration anyway, so an executable with seven drifting commands would have been allowed to start. A verdict nothing acts on is not a check.

Eleven of the twenty-four run **through the service**, `pio serve-opencode`: a turn that completes, who decided each tool use, a harness refusal told apart from a PIO decline, the two option-kind cases below, the usage census, and — the one that matters most — **a downgraded session refused before any prompt**. The host reaches `session/new`, sees the session report a different provider, refuses, and the view records `failed_before_delivery`; the fake's markers show `session_created` and **no** `prompt_received`. This adapter can refuse while the brief is still inside PIO, which the Claude adapter cannot.

`service_audits_a_tool_use_by_its_real_target` carries a defect the live runs found and the offline cases could not have. **ACP announces a tool call at `status: pending` with an empty `rawInput` and no `locations`**, and fills both in later `tool_call_update` messages keyed by the same id. The host collected only announcements, so every live tool use was recorded `not_classifiable` with a target digest that was the digest of `{}` — including, in R5, the out-of-fixture read PIO had just declined. The audit and the decision disagreed about the same call. The host now folds the updates together, takes the target from `rawInput` or from ACP's own `locations[].path`, and classifies it with the resolver the permission path uses; the harness's own final status is recorded beside PIO's verdict. The mutant that reads only announcements fails the case.

`service_asks_for_the_narrower_mode_and_checks_it_took` carries the only per-session lever that does not edit the owner's configuration. PIO asks for `mode: plan` and **refuses unless the session reports it back** — a harness that accepts the request and stays where it was is a refusal with the brief still inside PIO, not a success. Widening is refused before anything starts: `build` and any other value are `mode_is_not_a_narrowing_one` at admission.

`the_exception_covers_the_provider_and_nothing_else` carries the owner's widening of 2026-09-21: three different MiniMax models are admitted, and `opencode/`, `anthropic/` and `juspay-grid/` are each refused with the rule that refused them named. The `opencode/` rows matter most — that is what a silently downgraded session reports, so admission refuses what the session guard would.

`a_new_session_does_not_start_on_the_requested_model`, `service_refuses_a_selection_that_did_not_take` and `service_stops_when_the_model_selection_is_refused` carry the rule that **passing a model is not selecting one**. A new ACP session reports the harness's own default; PIO selects with `session/set_config_option`, reads the harness's own updated report back, and runs the guard on that rather than on the absence of an error. All three also assert that a refusal reaches a **terminal runtime** — before that fix an execution refused before delivery sat at `preparing` for ever and a caller could not tell it from a slow start.

`a_decision_is_selected_by_kind_never_by_id` and `a_decision_whose_kind_is_not_offered_is_refused` carry the rule that a permission option is chosen by its `kind` and never by its id. The fake's ids are `opt_1`, `opt_2`, `opt_3` and no id is its own kind, so the host's previous hard-coded `allow` and `reject` — the fake's own invention — select nothing and fail. When the needed kind is absent the host answers `cancelled`, the always option is still offered and still not taken, and the record that reaches the receipt attributes the refusal to **PIO**, not to the caller who asked for an allow.

`service_measures_where_the_harness_reports_usage` runs the same turn twice against a fake that reports usage once at the end and one that reports it after every message chunk, and asserts the census tells them apart by count. It also carries what R1 measured: OpenCode's `usage_update` names none of its fields `usage` or `*tokens` — it sends `used`, `size` and `cost` — so the census finds it by the update's own **kind** and records the whole update. `service_turn_completes` carries the other half: the turn result's usage is found at `usage`, not `_meta.usage`, and **every** counter is summed, including `thoughtTokens`. Both were wrong until a live turn said so, and a fake that reported the shape PIO expected could never have said it.

A run's receipt carries `delivery_proof_class: null`, because this harness returns no acknowledgment identifier (ADR 005 §7), and `owner_service_untouched`, which the host asserts before completing.

**Scope, stated plainly:** the other ten cases drive the adapter and the fake directly. Restart, reattach and host-loss are **not** yet covered for this harness the way they are for Codex and Claude Code.

**Cleanup kills process groups, not processes.** The harness is spawned with the arguments a real harness would get, so its command line names no store: killing only what names the store left `pio claude fake-cli` running. The host calls `setsid`, so it leads its own group and the harness is inside it. And once the host is killed the harness is reparented to init — it descends from nothing that names the store, so a walk cannot find it either. The groups seen while the host was alive are therefore remembered, and a remembered group is signalled only while a process it recorded is still alive under that group with the same command line, which is what stops a reused group id reaching a stranger. A matrix case proves it: the fake ignores signals, the host is killed from outside, and cleanup must still leave nothing.

`python3 scripts/case_cleanup.py` is a **self-test of that guard, run in CI**, because its first version passed locally and refused every store on Linux: it rejected roots with fewer than three path separators, and `/tmp/pio-x` has two on Linux but three on macOS, where `realpath` prepends `/private`. The guard now requires a path strictly under a permitted prefix, and the self-test checks that a private `/tmp` store is removable while `/`, `/tmp`, `/private/tmp`, `/usr`, the home directory, the working directory and a world-readable directory are all refused.

**`release()` validates before it kills.** It refuses any root that is not a private directory this run created — outside the permitted prefixes, not owned privately, a symlink, or containing the home or working directory. The guard runs *first*: validating after killing means a wrong root such as `/` matches every command line on the machine and signals every process group before anything refuses it. Writing it the other way round killed this developer's own shell, which is the cheapest demonstration available.

**Both service-backed matrices clean up after a failure, not only after a pass.** Each case registers itself and is released in a `finally`, because cleanup on the success path alone left six daemons running, parented to init, and 949 store directories on `/tmp` during development. A deliberate failure now leaves no process and no directory behind, and that is checked by forcing one.

## The live runners

`scripts/claude_live_run.py` and `scripts/opencode_live_run.py` drive the owner's installed harnesses through `pio serve-claude` and `pio serve-opencode` exactly as the plans on issues #7 and #10 state. **Both take `--dry-run`, which drives the labeled fake through the same service and the same code path**, so CI exercises the runners themselves with no harness installed and no model call.

Every submit carries a **120 second delivery timeout and a 600 second execution deadline**, and the deadline stop is the one already built into the host.

The cap's measure is **input, output, cache creation and cache read, summed, and reported separately** in every receipt. ACP reports input and output only, and the receipt says so rather than implying the other two were zero.

**A dry run is separated from the live tree entirely.** It writes under `--out`, with its own settings directory, its own store and its own transcript, so it can never append to a file a live receipt comes from. Every live run gets a **fresh private directory keyed by the run and a new id**, never reused.

**The runners refuse rather than record.** A live run from a dirty tree is refused, not noted — M2 produced one such receipt and it had to be preserved, disclosed and re-run, which is what recording alone buys you. A binary older than the last commit touching `crates`, `Cargo.toml` or `Cargo.lock` is refused too, because the receipt would describe code that is not what ran. Every receipt carries the binary's SHA-256.

A dry run uses **its own stand-in settings directory**, never the owner's. On a machine with no `~/.claude/settings.json` the permission-mode guard correctly refuses an absent default and the service never starts — which is what CI showed — and a run exercising the runner has no business reading the owner's configuration anyway.

Stop rules are applied to every finished run and abort the sequence: no usage report (recorded as **unknown, never zero**), a usage report that parses to zero while the harness sent something, an effective permission mode that did not match the requested one, a change to the owner's settings, the owner's OpenCode service moving, or cumulative usage reaching 80 per cent of the cap.

For OpenCode the first of those has one exception, measured rather than assumed. R4 established that a cancelled turn reports **no usage at all**, so a run whose brief exists to be cancelled is charged an allowance of 12,000 tokens instead of halting the sequence; the figure's basis is in ADR 005 §10 and in the ledger itself. **An unplanned missing usage report still halts.** The ledger carries `observed` and `charged`, and **every stop rule uses `charged`**.

The zero-parse stop exists because the first OpenCode dry run produced a receipt claiming a *reported* usage of zero: ACP spells its counters in camelCase and the measure read the Claude spellings. A reported zero is indistinguishable from unknown and worse than either.

## The shared host lifecycle

Codex, Claude Code and OpenCode differ in the protocol they speak and in nothing else that matters to the host. `pio_host::harness::Lifecycle` holds the part that is literally shared, once: detach and the M1 launch fences (invocation identity, no launch already recorded, host slot free), the claim, the guard event that refuses a request broader than the user's configured default, the spawn marker binding the child to the qualification record, the park, the **release gate** held across the first native write, the append-only event file, the control file with at-most-once application, the deadline stop, and the receipt or the known-not-released failure.

What a harness says on the wire stays in its own module; the lifecycle never parses a harness message. The event and control files keep each adapter's own prefix, so extracting this changed no path the service reads.

**The proof that the extraction changed nothing** is that every existing count is unchanged: the offline Codex matrix is **51 of 51** across its 17 cases, the public Unix process matrix is **72** (51 pass, 6 expected property failures, 9 expected defense refusals, 6 expected classifier failures), the diagnostic fake-host matrix is **54** (39/3/9/3), and caller recovery passes.

## M3 Claude Code qualification

`pio-claude` binds the user-selected Claude Code executable before any native work. `target/debug/pio claude qualify --executable PATH --work DIR [--expected SURFACE]` resolves the path to the binary that actually runs, hashes it, requires version **2.1.281** (re-pinned from 2.1.278 on 2026-09-26, after the cask self-updated), and then digests the command-line surface: the top-level help plus seven subcommand helps, compared with the checked-in [surface identity](../adapters/claude/2.1.281/surface-identity.json). `pin_names_the_identity_directory_and_the_requalification_record` fails if the pin, the two compiled-in identities and the committed re-qualification record for that version do not name one release. Claude Code publishes no schemas, so the surface is what the adapter can pin; it is byte-identical across runs and costs 50 ms, which keeps re-qualification cheap for a cask that self-updates. Exit 0 when qualified and 3 when refused; refusals are data (`unresolved_executable`, `version_unavailable`, `unsupported_version`, `surface_drift`). Every invocation uses an isolated `CLAUDE_CONFIG_DIR` and an explicit environment with no credential variable.

A help digest cannot see the wire, so a second artefact is pinned: the [stream identity](../adapters/claude/2.1.281/stream-identity.json) — the `system/init` key set, the capability list, the message sequence, the `result` key set and the product's own default mode and model.

`target/debug/pio claude auth-route --executable PATH (--home DIR | --isolated DIR)` observes which credential route is configured and exits 3 when none is usable. `--home` observes the route the user actually has; `--isolated` is the negative control, and the two differ in exactly one thing so a refusal cannot be an artefact of the environment. The child environment is an allowlist — `PATH`, `HOME`, `USER`, and `CLAUDE_CONFIG_DIR` only when isolating — because measured, a child without `USER` reports a working login as absent. It records only `loggedIn`, `authMethod`, `apiProvider` and `subscriptionType`; `auth status` also prints the account's email and organization, which are dropped at that boundary and appear in no record. `target/debug/pio claude settings-snapshot --config-dir DIR` reports the configured permission mode, the allow-entry count, the model, the always-thinking setting and the enabled plugin names, with a digest of the file.

Cargo tests use labeled fake executables, so CI needs no Claude Code: surface digesting and its listing, drift naming changed, added and removed commands, qualification in an isolated configuration, an unsupported version that never receives Claude-specific arguments, a missing executable refused as data, the route recorded with account identity dropped and proven absent from the record, a missing route reported unusable, a route observable only when `USER` is passed, the permission-mode guard (an absent `defaultMode` compared as the product default, D2), the settings snapshot across both settings files, both checked-in identities, stream drift naming the field that moved, a run's `init` held to the pinned key set and capabilities with both digests recorded (`init_is_held_to_the_pinned_stream_identity`, D11), the single-use decision allowlist with the widening fields absent, and every tool use recorded with targets outside the fixture flagged and a shell command reported as not classifiable rather than assumed contained. `without_a_result_a_denied_use_stays_denied_and_the_rest_are_unknown` covers a turn that ends with no `result`: PIO's decline and a caller's deny stay denied, everything else is `unknown` with the liability unresolved, never `performed` (D3).

`target/debug/pio claude fake-cli` is a **labeled fake Claude CLI**. It speaks the stream shapes measured from 2.1.278 over stdio — including the awkward one, that `system/init` is not emitted until a message reaches stdin — but runs no model, tool or command, and labels everything `pio-fake-claude-cli`. It answers `--version`, `--help` and `auth status` as well, so one executable can be qualified, drifted and driven. A scenario in `PIO_CLAUDE_FAKE_SCENARIO` selects the version, a help suffix that moves the surface digest, the credential route, a permission request, the tool uses reported and the usage total.

It **records** a permission response that carries a widening field rather than refusing it, so the matrix proves PIO never sends one instead of trusting that it does not; one test drives that detector deliberately to show its absence elsewhere is evidence. Integration tests cover a completed turn with the replay echoing exactly what was sent, nothing emitted before input, an allow that is single-use with the input unchanged, a counted deny, the widening detector, tool uses recorded with an out-of-fixture target flagged, and the non-streaming surfaces.

`pio serve-claude --data-dir DIR --config FILE --socket PATH` is the Claude service. It validates a `pio-claude-service/1` configuration, runs every pre-spawn decision through the same admission code the offline matrix exercises, writes the admission record beside the store, and **refuses to start** when that record refuses. `pio claude host` is launched only by the service controller, through the same `submit_harness` path, launch guard and host-slot fence the Codex host uses.

`target/debug/pio claude service-admit --config FILE --work DIR` makes every decision a service must make **before it spawns a turn**, in a recorded order: the settings allowlist, the environment (which must carry no credential variable and nothing outside `PATH`, `HOME`, `USER`, `CLAUDE_CONFIG_DIR` — plus the fake's scenario variable, accepted only when `labeled_fake` is true), the dated test-only model exception, the permission mode against the user's own settings, qualification, and the credential route. Exit 3 on refusal, with the reasons as data. The record states `stream_spawned: false`: observing the route does run `auth status`, which makes no model call, takes no brief and starts no session, and the record says so rather than implying it.

`python3 scripts/claude_host_matrix.py --out DIR --repetitions 3` is the **offline Claude matrix**, run in CI. Eleven cases, each three times, against the labeled fake: a turn that completes; the replay acknowledging delivery by exact echo; a permission decision denied and allowed; an out-of-fixture request **classified and declined by PIO** with the tool use still recorded; an unclassifiable request surfaced rather than auto-allowed; a widening decision never sent; and, before any spawn, an unqualified executable, a missing credential route, a permission mode that is not the configured default, and surface drift — each refused. The out-of-fixture case uses an absolute path that begins with the fixture and then climbs out of it, because a relative one would be declined even by a broken classifier and would prove nothing. It also asserts that a **sibling fixture is outside**: it needs no traversal, and a boundary drawn at the area fixtures are created in — which is what the host passed until `6026436` — would have called another run's workspace contained. The drift case pins the fake's own surface as a baseline and then moves one help, so it proves drift rather than merely proving the fake is not Claude Code.

Fifteen of the twenty-eight run **through the service**, `pio serve-claude` over the public Unix API, which is the only place journal, delivery and usage behaviour can be observed:

- a turn that completes, whose view carries `delivery: acknowledged` with evidence class `native_replay_echo` and **no proof class** (D7: the echo returns no identifier; `only_a_returned_identifier_earns_a_delivery_proof_class` is the unit test), `exit: {code: 0}`, the containment statement, and an observed `claude.tokens.total`;
- a real configuration pointed at an unqualified executable, where the service **refuses to start at all** and the admission record says `stream_spawned: false`;
- a control request PIO will not act on, which still receives the protocol's **error control response** — recording a decline while sending nothing would leave a real harness waiting forever, and the fake records that it was answered;
- a harness that **ignores the interrupt**: SIGINT, a bounded wait, then SIGKILL, with the escalation recorded and usage left **unknown rather than zero**, because a killed child sends no `result`;
- a **daemon restart**, which reattaches to the running host and never re-sends the brief: exactly one `turn_received` marker across both generations;
- a **host lost after release**, where the brief was delivered and then the host and child were killed. Delivery stands, because the replay echo already proved it; the runtime becomes `unknown` and the exit `unavailable`; nothing is re-sent and no second harness is started;
- the **transcript the run wrote**, reported. The fake writes a session file and a `memory` directory under a slug of the session's working directory, as the real harness does; the case asserts the before listing is empty, the after listing holds both entries, and the diff reports two new ones. It fails if the snapshot slugs anything but the workspace, which is the defect R1 disclosed;
- **who decided each tool use, and what**, asserted on the record that reaches the receipt rather than on the events. Four situations through the service, by `tool_use_id`: a request nobody answers is PIO's; a caller's deny and a caller's allow are the caller's, the allow ending `performed`; and PIO's own decline of an out-of-workspace target is PIO's, with no out-of-fixture effect observed. Both fakes announce the tool use they are asking about, as the real harness does, and the Claude fake lists **every** refusal in its denial list rather than only the ones it made itself — without that there was no record to attribute anything to. The same four run in the OpenCode matrix;
- a **request nobody answers, denied by default**. The case surfaces a permission request, answers nothing, and asserts that after the caller's own declared delivery timeout the host sends a single-use deny recorded as `request_denied_by_default`, with one suggestion offered, none acted on and no widening field sent, and that the turn then exits 0 rather than hanging. With the expiry disabled the execution sits in `requires_action` forever, which is the state it prevents;
- a **harness refusal told apart from a decline**. A host is attached and the harness refuses anyway, because its own rules shadow the callback; the case asserts PIO declined nothing, was asked nothing, and that the view carries `containment.denied_by_harness` with the reason — so a caller can tell "the harness refused" from "PIO declined" from "a caller decided". A twelfth case, driven directly, reproduces R3b: with no prompt tool and no handshake the harness denies by itself with the measured text and PIO never sees a request, while the same case with a host attached does reach PIO;
- a **cancelled turn reported as unknown, not zero**. The fake answers SIGINT the way the harness was measured to, with an aborted `result` whose usage block is empty; the case asserts the host records `usage_unknown`, publishes no observation, and leaves usage liability `unresolved`. It fails if the empty block is passed on as an amount, which is what R5 caught;
- a run under the **product default mode, when the user configures none** (D2): admission compares the requested `default` with the measured product default and records `configured_source: product_default`, the fake records that **no** `--permission-mode` flag reached it (the flag does not accept the name), and the `init` echo matches; `acceptEdits` under the same settings is still refused;
- a **stream identity drift refused at `init`** (D11): the fake's `init` carries one key the pinned release never sent and then asks for a permission. The host records `stream_identity_checked` with the observed and pinned digests, reads only the replay echo that follows, stops the child and records `stream_identity_refused` before any tool use is answered: no action is surfaced, the fake receives no decision, and the view shows `delivery: acknowledged`, `exit: unavailable` and usage liability `unresolved` with no observation;
- the **configured model, read from the user's settings**. The host took it from a service-configuration field the admission list does not permit, so it could only ever be absent and R1's receipt reported no configured model while the settings named one.

That last case caught a real defect. The provider's adapter-switch check, which stops a restarted daemon resuming someone else's executions, knew only about Codex, so a restarted Claude service refused its own work with `cannot switch persisted execution adapter mode`. It now reads the expected source from the harness table.

**Scope, stated plainly:** the other eleven cases drive the adapter and the fake directly, so they prove the adapter's decisions and the stream shapes rather than the durable host's behaviour.

`python3 scripts/claude_requalify.py --executable PATH --out FRESH_DIR` needs an installed Claude Code and is not part of CI. It is the single re-qualification command: it compares both pinned identities, rerunning the zero-token probe to reach the stream half, and exits 0 clean, 3 on drift and 4 if the user's configuration moved under it. It makes **no model call** and leaves the user's configuration byte-identical, which it checks across `settings.json`, `settings.local.json` and `~/.claude.json`. With `--isolated` no child sees the user's configuration at all — the version, helps and `auth status` also run with a scratch `HOME` and `CLAUDE_CONFIG_DIR` — so the route is not observed and the summary says `not_observed` instead of comparing files it never read; the 2.1.281 re-pin was measured that way, and its drift from 2.1.278 is recorded. Findings are in [claude-qualification](work/m3/claude-qualification/README.md). None of this is a live run, a real journey or a model-backed result.

`the_recording_boundary_redacts_a_home_path` in the OpenCode matrix covers the **boundary**, not the function. The reviewer's mutant — replacing `redact_home` with a clone at both `Lifecycle::event` and `Lifecycle::complete` — survived 102 tests and all three matrices, because only the function had a test. The case has the labeled fake report a home-shaped path in a tool call's `locations`, and asserts the stored events carry `~/private/notes.txt` and no home path in any form: a host that dropped the field instead would not pass either. A second run has the fake refuse a session with a path-bearing error, which is the **only receipt that can carry a path today** — `Lifecycle::fail`'s `known_not_released` reason — and it was the one site redaction had been added everywhere except. Both mutants now die at a named assertion.

No *completed* receipt carries a path today, so the assertion on that one cannot fail on its own; it is there to fail the day one does, and the failed-run half is what actually covers a stored receipt. The launch spec beside it is deliberately **not** redacted: it is how the host finds the harness, it is internal state, and only receipts are published.

`PYTHONPATH=scripts python3 scripts/board_fold.py --out DIR` proves the M4 board needs **no new operation** (gaps G1 and G6). A second caller, attaching after six runs already exist and holding its own scoped grant, enumerates every execution it may see with `core.events.read {from: "start", kinds: ["execution.execution"]}`, subscribes for the changes, and calls `execution.inspect` **only for subjects whose revision moved**. Measured: six runs discovered without being told a single id, six inspects for the first draw, then **one** inspect per changed subject in each later round — the six settled runs are never read again, which is G6. The grant is a boundary rather than a label: the same caller's `execution.submit` is refused, because its rights are `core.events.read` and `execution.read` and nothing else.

It runs three more passes. **Retention:** a service keeping only its last four events cannot supply six runs from the stream, and hands back a `retention` gap whose `snapshot.subjects[]` carries `{subject, revision, state}` for each — a roster, not a hole. **Push:** one round folds because a notification arrived rather than because a timer fired, which is the path the screen actually uses. **Scope:** a grant with `id_prefix: run-1` sees only that run, the result carries `filtered: true` so the caller knows something was withheld rather than absent, and `execution.inspect` on another run is refused `permission_denied` / `out_of_scope` — the exact shape the M4b lead's grant will have.

The sharpest assertion is the one that is also deterministic: **every settled run is inspected exactly once across the whole session.** A run that moves again may be read again; a run that does not move is never re-read.

`--mutant naive` is the board that re-inspects every subject on every tick. It draws the same screen and fails on `['run-1', …, 'run-7'] != ['run-7']` — the only thing separating a fold from a poll.

Two things the proof established that the schemas do not say out loud: `core.grants` must be negotiated to **present** a grant, not only to issue one, or the envelope is refused `invalid_envelope` at `/grant` before authorization is reached; and a retention gap hands back `snapshot.subjects[]` with `{subject, revision, state}`, so a client attaching late is given a roster rather than a hole.

`PYTHONPATH=scripts python3 scripts/lead_runs.py --out DIR` proves `origin` and the lead's grant on `serve-fake` and `serve-opencode`, with thirteen `--mutant`s that must each fail their own assertion. These cover: depth derived, not claimed; the budget counted from the journal; the initiator bound to the grant; an omitted origin refused under a subtree grant; an initiator that was refused or has exited starting nothing; under a grant, an initiator PIO has never seen refused `initiator_unknown`; a root run refused under any grant (`owner_authority_required`), because a root is the owner's act; a grant that names no subtree (the whole kind, a bare prefix with no separator, two runs by id) is not a lead's: it may start a run with no lineage, as Protocol conformance requires, and may claim none (review 46); steer grantable; and an answer refused `right_missing`.

`PYTHONPATH=scripts python3 scripts/lead_run.py --rehearse --out DIR` is the L1 lead run against the labeled fake, the same code path the live run takes. It checks 32 rows, each with an expected value that fails the run on a mismatch, and two records (the second, what PIO declined by itself, since `12fe606`), and marks the rows a rehearsal cannot prove. Three rows came from L1's live run (review 46): the live tree can be released (a rehearsal makes a probe under the live tree's base through the same code, checks it, and removes it; the live run refuses to start if its tree could not be released); the runner's own steps raised no error; and the service was released with nothing it started surviving. Before the service starts, each run's worst-case share is reserved in the ledger (a rehearsal's own); every exit replaces the reservations with the charges and writes the receipt, including an exception, `SystemExit`, `KeyboardInterrupt` or `SIGTERM`. A watchdog in its own session stops the service if the runner dies without its exit path. Its seventeen `--mutant`s each exit non-zero and print `dies on '<row>'`; fourteen also leave a receipt and a charge, among them `lead-submit-invalid` (the lead's submit is answered with an error, so no execution exists, and it is charged nothing, not its share) and `third-admitted` (a budget of three, so the runner's third start is admitted: it is cancelled and charged its share on a ledger line of its own); and `setup-fails` must leave no receipt and no tree (a setup that fails before anything is reserved releases the tree it made), `helper-elsewhere` must be refused before anything is reserved, with no receipt and no tree (it puts `small_model` on another provider in the rehearsal's own OpenCode configuration; owner decision, 2026-09-25: the runner refuses to start if `small_model` or any `agent` or `mode` model names a provider other than the plan's), and `runner-killed` must leave no receipt, every reservation standing, and the service stopped. `release-refused` has the cleanup's guard refuse the tree at the end, then releases it for real. After a SIGKILL the reservations (1,395,200) stand and count as charged, so the next attempt is refused by the 1,600,000 stop; replacing them is the owner's dated act, never the runner's. A live run is charged **measured from the store's recorded steps**, which is not the bill: that the store holds every billed call is not proven, and the owner reconciles against the MiniMax console. `desk-silent` waits out the 300-second delivery timeout. The live run needs `--desk "<the owner's words>"`, refuses a dirty tree, builds the binary itself, holds a no-sleep assertion, refuses a ledger name it already holds (`--attempt` gives a spent run a new one), and refuses to start unless the sequence's charged total plus the attempt's worst case stays under the 1,600,000 stop.

`PYTHONPATH=scripts python3 scripts/lead_run.py --rehearse --plan L3 --out DIR` is L3, on Codex, against the labeled fake app-server. The approvals are the owner's of 2026-09-24 and 2026-09-25. The shape is L1's: one lead, budget 2, and two children, each running the one command its brief quotes (`sleep 30 && wc -l alpha.md`, `sleep 5 && wc -l beta.md`). Limits (the owner's of 2026-09-26, "More headroom", and for each child Q9, "Child 90k"): the lead's ceiling is 150,000 and each child's 90,000, and a step is at most 30,000. Codex reports a step once its tool has finished (M2 R5, R6), so a report that crosses a ceiling arrives with the next step begun: a child can reach its ceiling plus two steps (150,000). The lead's tool withholds any response once the lead has reported more than its ceiling less one step (120,000), and the runner then stops it; the meter also stops the lead when a result its tool never saw comes back past 120,000. That keeps a lead using only its tool within its ceiling plus one step (180,000), but a result the tool never sees starts a step before anything can stop it, so the lead's share is its ceiling plus two steps too (210,000; review of L3, round 2, SB-1). The worst case is therefore 510,000, reserved immediately before the lead's submit. For the second attempt (owner, 2026-09-26, Q9) it is within the sequence's 548,000 stop (cap 685,000) after attempt 1's 36,126, and, with the Codex ledger's 457,576, under the Codex stop of 968,000 (cap 1,210,000); `lead_run.py --sizing-selftest` checks both and that a third attempt is refused. Under Q8, earlier that day, it was 450,000 against 488,000 (cap 610,000) and 908,000 (cap 1,135,000); attempt 1 was checked against 452,000 (cap 565,000) and 872,000 (cap 1,090,000). Under the limits of 2026-09-24 it was 405,000, past the 320,000 stop, and a live run was refused; a rehearsal reserves it on its own ledger. The lead tool rides on the lead's `thread/start` config with its own two tools pre-allowed. Every thread's model and provider (`gpt-5.6-terra`, `openai`) and `approvalsReviewer: user` are checked from Codex's own answer before its first turn. A run that ended by itself is charged Codex's reported total, which covers every step (review 48). One the runner stopped is charged the most PIO saw it spend (its report, or its own meter's total if higher) plus one step in flight, not capped at its share, so a run that spent past its share is charged past it and fails the share row; one not seen exited, its share or what it was seen to spend plus a step, if more; one stopped for silence or with no usage, its share or what it reported, if more; a submit that made no execution, nothing; and a probe that should have been refused but was admitted is cancelled and charged on a line of its own, through the same branches as any run and never below its share: at least its share, and at least what it was seen to spend plus a step in flight; the floor and share rows cover it (review of L3, round 2; round 4, SPEND-12: `probe-over-share` fails the share row with the floor holding, and `probe-charged-flat`, its old flat share, fails the floor row). No store is read. Codex runs are metered on the event stream by a thread of their own, apart from the watch loop, which also tails the lead's host events for results its tool never saw; a run active for 150 s with no usage is stopped rather than run unmetered, and one that is asking at the desk is not silent. Before anything is reserved, right after the service starts, the runner compares the Codex that `serve-codex` qualified with the committed record and refuses a different one, and refuses to start if the Codex configuration turns on `features.exec_permission_approvals` or `features.request_permissions_tool`, by its own key or a legacy alias, recording which key decided. Live, it also refuses unless every unmetered, default-on feature of Codex 0.157.0 is off for L3's threads (review of L3, round 4, U1): sub-agents, memories, goals, standalone web search and image generation, each spending outside every report PIO reads. Each must resolve off from the owner's own `config.toml` or from the per-launch override under a recorded owner decision (the owner's of 2026-09-26, which the L3 plan sends for its own threads; the keys are `pio_codex::features_off`), laid over the owner's keys as Codex lays a request override, and resolved as rust-v0.157.0 resolves it: a feature's own key and legacy aliases in sorted order, the last deciding (`memory_tool` after `memories`), `imagegenext` ignored beside `image_generation`, the top-level `web_search` deciding the mode, and sub-agents off only with `agents.enabled` false and `multi_agent_v2` not on. It reads those keys and nothing else, names each feature not off and the key that decided it, refuses before anything is started or reserved, and records each feature's route (the owner's configuration, per launch, or Codex's default). The lead's tool starts only the plan's children, and the runner meters, stops, cancels and charges every execution under the lead the stream shows, whatever its name. It checks 44 rows and three records. Sixteen rows are new for L3: the approvals are asserted to go to the user; Codex was qualified at the pinned identity (inconclusive against the labeled fake, which has no qualification); the lead's two tools were pre-allowed and nothing else, on the wire, with nothing asked about them by any path; a steer under the lead's grant on running `alpha` is `recorded` and then acknowledged by Codex (`provider_ack_id`, behavior `not_observed`); every desk answer went out as a single use and PIO declined nothing by itself; every stop the runner made reached Codex (the host's own `turn/interrupt`, acknowledged, the turn interrupted; inconclusive when there was no stop); every result the lead got was checked against its hold (the tool's own log, one gate line per response, error or success); every run's charge covers what it could have spent (a floor worked out from the views and the meters, not from the charge); every run's charge stayed within its reserved share; every step stayed within the in-flight bound (each report's last step and the rise between reports, from the host's own usage events); no run spawned a sub-agent; every run that spawned one was stopped when it appeared (inconclusive when none did); no run took a turn of its own after its turn ended (round 4, SPEND-9: a goal's continuation, which the host interrupts, its run then charged as cut short); Codex's memory pipeline wrote nothing during the run (its files and databases listed before and after, by a Codex-chosen name or a digest, size and time, never content); each run reported its usage; and the owner's Codex configuration changed only by the fixture's trust entry. The records are what Codex asked and what was sent, what PIO declined by itself, and the stream retries Codex reported per run, with why none is charged (round 4, SPEND-10). Its mutants `wrong-model`, `reviewer-elsewhere`, `no-pre-allow`, `child-overspends` (which must also hold the stop and charge rows), `lead-asked-in-openai-form`, `child-asks-permissions`, `service-never-ready`, `first-number-of-all`, `ceiling-cancel-never-sent`, `stop-charged-reported`, `usage-suppressed`, `lead-heavy`, `qualified-elsewhere` (refused before anything is reserved), `lead-asked-by-user-input`, `lead-tool-error-past-hold`, `lead-shell-past-hold`, `tool-error-ungated`, `meter-ignores-items`, `stop-ignored`, `not-exited-charged-share`, `experimental-feature-on` and `experimental-alias-on` (refused before anything is reserved; the second by Codex's legacy alias `request_permissions`), `asked-silent`, `lead-submit-invalid`, `third-admitted`, `child-renamed` (the lead's tool refuses a child the plan does not name, so the start row fails) and `child-renamed-unchecked` (the renamed children let through the tool: the runner meters them from the stream, stops the renamed `alpha` at its ceiling and charges both on lines of their own), `subagent-spawned`, `subagent-not-stopped` and `subagent-uncounted` (round 3, SPEND-2), `subagents-unguarded`, `subagents-multi-agent-only`, `memory-unguarded`, `memory-alias`, `goals-unguarded`, `web-search-unguarded`, `image-generation-unguarded` and `override-misses-alias` (round 4, U1: each refused before anything is reserved, for its own feature and no other), `decision-absent` (no decision over the owner's own keys as read on 2026-09-26: refused for all five), `step-past-in-flight` (SPEND-4), `stopped-past-share` and `stopped-charge-capped` (SPEND-7), `deadline-interrupted` and `interrupted-charged-reported` (SPEND-6), `memory-pipeline-ran` (round 3, S6: Codex's memory pipeline writing under the Codex home), `goal-continued` and `continuation-uncharged` (round 4, SPEND-9: a turn Codex starts on `beta`'s thread after `beta`'s has ended; charged as ended by itself, the floor row fails), `late-step-after-halt` and `stale-meter-charged` (round 4, SPEND-11: a step the host reports after the meter has stopped folding, during the cancels on the way out; the charge reads each run's host events afresh from the files, as the floor does, and one that reads the meter's fold alone fails the floor row), `meter-dies` (SPEND-5: the meter thread fails, and cancels every run itself) each fail their named row, `lead-exit-uncarried` (C3-3: the lead's exit carried no list of what PIO declined, and it was not seen to exit) and `owner-service-absent` (C3-4: no OpenCode service of the owner's before or after) must each leave their row inconclusive, `overrides-on` must hold "No run spawned a sub-agent" (every feature off per launch over a configuration that turns each on, the whole set on every thread, no agent spawned and no memory pipeline run), `qualified-as-committed` must hold its row, and so do L1's own, played on Codex (all in CI except `desk-silent`). `helper-elsewhere`, `no-ask` and `alpha-outlasts` belong to the OpenCode plans. `python3 scripts/l3_desk_relay.py --selftest` checks the live relay (owner decision of 2026-09-25, item 3). By itself it answers only `L3.alpha`'s and `L3.beta`'s command approvals for exactly their own `sleep N && wc -l` command, bare or as Codex names it (`/bin/zsh -lc '…'`, measured in M2), and only inside the run's fixture with no network ask, and `L3`'s own `pio-lead` `start_run` and `read_run` approvals if Codex asks despite the pre-allowance. It leaves seventeen other requests for the owner, among them the exact command inside the fixture with no network flag and with a flag of null (a missing flag is not "no network"), and another server's elicitation with pio-lead's exact words and approval kind (round 4, U3: at rust-v0.157.0 an MCP server's own form elicitation keeps its own message and `_meta`, so only the server check leaves it to the owner; CI runs the selftest against a copy with that check removed, which must fail), and the selftest runs the relay over a desk holding an empty pending file beside a valid one: it answers the valid one and does not die. `PYTHONPATH=scripts python3 scripts/lead_run.py --charge-selftest` checks the Codex charge's corners that no play reaches, on made-up runs: stopped for silence past its share, interrupted by the host alone, stopped past its share (uncapped), a step past the bound, and a sub-agent. `python3 scripts/lead_tool.py --selftest` checks the lead tool's gate: it does not judge a response on a meter reading older than 1.5 s after the call arrived, a fresh reading past the hold withholds it, and a fresh one under the hold hands it back. `lead_run.py --rehearse --plan L3 --relay` is a rehearsal whose desk waits for answer files, as a live one does. CI runs the relay beside it: the relay must answer `beta`'s request in advance, bring nothing to the owner, and every row must hold.

`PYTHONPATH=scripts python3 scripts/lead_run.py --rehearse --plan L1b --out DIR` is L1b: L1's shape, with `alpha` briefed to work thirty seconds before it counts and `beta` briefed to read a placeholder `beta.env` with OpenCode's read tool. Two rows are added: a `read_run` of `alpha` that waited (at least 20 s by the tool's own log and returned `exited`, or returned still running after the tool's 55 s limit); and the desk row, now with an approval to decide. Review 47's rule applies to both plans: a desk nobody asked, and a session listing that was empty before, can decide nothing, so those rows are inconclusive, never passed. Mutants: `no-wait` fails the waiting row; `no-ask` must leave the desk row inconclusive; `alpha-outlasts` must leave the waiting row **holding**, through a read that came back still running at the limit and through nothing else.

`python3 scripts/check_private_paths.py` refuses any absolute home path in **any committed file**, text or binary, and runs in both workflows. This repository is public: a receipt, an event file and a test fixture are all published, and the owner's home directory is not ours to publish — nor is what such a path discloses. The Claude receipts carried the filesystem path of every installed plugin from R2 onward, 117 paths across 16 files, until 2026-09-21.

Redaction happens at the **recording boundary** rather than here: `pio_core::redact_home` runs in `Lifecycle::event` and `Lifecycle::complete`, shared by all three adapters, and in both live runners before a receipt is written. It removes the invocation's own home and any other account's home directory, by shape rather than by name, object keys included. `--selftest` plants a path in a scratch tree and proves the check fails on it, in text and in binary, and that a redacted path and a deliberately fictional one pass. A gate nobody has seen fail is not a gate; this one caught its own Rust test, which had been written with a real home path in it.


## M4 T1 — the public client and the command line on it

The M4 screen's first rule is that it reads and writes **only through the public API the command line uses**. `crates/pio-client` is that API in Rust, and `pio client` is its first consumer.

`cargo test -p pio-client --test dependencies` holds the boundary four ways, because the first cut checked one and the verifier went round it twice (review of T1). **The graph**, read by `cargo metadata --all-features`: pio-client may not reach `pio-core`, `pio-protocol`, `pio-host` or an adapter crate by any path, direct or transitive (dev-dependencies aside), and the same test asserts the CLI, which does reach the service, is seen reaching it. **An allow-list** of pio-client's own dependencies: none by path, none from the workspace, none new without writing it down. **The sources**: no module path attribute and no include macro that reaches outside the crate. **The command line**: `client_cli.rs` and `ledger.rs` name no service crate and borrow nothing from the rest of the binary; that check found `ledger.rs` calling `pio_host::secure_root`, and the ledger has its own now. The strict encoding/1 parse is `pio_client::encoding`, so the ledger parses a request file as strictly as the service does (a duplicate key refused) without `pio_protocol::encoding`, which is private again. `python3 scripts/client_source_mutants.py` runs seven mutants in a worktree of HEAD (`scripts/source_mutant.py`, one shared target), each required to fail for its named reason: `direct-pio-core`, `transitive-pio-protocol`, `B1-optional-feature` (an optional pio-core behind a feature, re-exported), `B2-path-include` (a service source file compiled in by module path), `cli-borrows-service`, `ledger-lenient` (the ledger parsing with serde_json) and `watch-notices-repeat`.

`cargo test -p pio-cli --test client_api` drives every operation the client covers against a real `serve-fake`: describe, capabilities, discovery, a subscription whose push is kept rather than dropped, submit, inspect, output read to the end, reconcile, events paged to an empty page, a grant that lets its holder read and not submit and, once revoked, not read either; the authority epoch at 0 before any claim and 1 after, a command fenced at the old epoch refused `stale_authority_epoch`, one at an old revision refused `precondition_failed`; and steer and answer refused `unsupported_required_feature` by a service that offers neither. `fenced` re-reads a run's fence only when the service answers `precondition_failed` with `retry: after_reconcile`, and never replaces a revision or epoch the caller pinned.

`python3 scripts/fold_parity.py --out DIR [--record]` holds the three client-side folds in Rust (`pio_client::board`, `walk`, `blocks`) to the Python folds they were ported from (`board_fold.Board.absorb` and `board_view`, `approval_desk.walk` and `decisions`, `transcript_blocks.Transcript.feed` and `fill`): the same recording goes to both, and the output must be identical field for field. The 19 committed recordings in `crates/pio-client/tests/fixtures/` came off the public socket of `serve-fake` (with and without a retention gap) and of `serve-opencode`, `serve-claude` and `serve-codex` behind their labeled fakes; between them they carry the states `needs approval`, `running` and `finished` (with and without the `usage unresolved` marker: unresolved usage is a marker, never `uncertain`, by the orchestrator's decision of 2026-09-26; `run_state`'s unit tests cover `uncertain` and `unknown`), a walk whose deadline and arrival orders disagree, every decider (`caller`, `pio` by lapse and by decline, `harness`, `nobody`, the last two with `decision: null` and `sent: false`), and a transcript with its audit and one (Codex) without. `--record` records fresh ones as well; `tests/folds.rs` holds the Rust fold to the Python fold's recorded answer in `cargo test` alone. `--mutant NAME` builds the fold from a worktree of HEAD with one field broken and requires parity to fail **at that field**: `walk-drops-offer` (`if_nobody_answers`), `board-exited-running` (`state`), `board-liability-uncertain` (`state`: unresolved usage made uncertain again), `blocks-nobody-unknown` (`decided_by`). Committed recordings name no machine's `/tmp`: `--record` rewrites every case root, in the recording and inside the transcript bytes, to `/tmp/pio-fixture`.

Porting found one defect in the Python: `approval_desk.due` read `requested_at`, a UTC instant, as local time (`time.mktime`), which moved every deadline by the machine's offset and changed no order, so nothing had noticed. It uses `calendar.timegm` now.

`python3 scripts/client_cli_matrix.py --out DIR` drives the `pio` binary as a person or a script would, and checks what it printed and its exit status against what the Python test caller reads on the same socket. Eight cases. On `serve-fake`: runs submitted through the caller ledger, the board drawn running and then finished, `client inspect` equal to the service's own view, `client output --follow` byte-identical to the spool; four watchers over one state file, detached by `--max-events` inside a page, by `--idle-exit`, and by SIGTERM while a run starts, with a run started while nobody watched, which between them print every event of the stream exactly once and in order; a position saved against one store and used against another, dropped with a notice and the new stream printed from its beginning, rather than `invalid_cursor` on every run; and the ledger's exit codes: an `internal_error` on submit is not known yet (exit 4, "pending … run pio client reconcile"), the reconcile replays it (0), a refusal with `retry: no` exits 3 with the error object verbatim. On `serve-opencode` with its labeled fake (serve-fake advertises no approvals and cannot cancel): two runs waiting, drawn as `needs approval` and walked with their options and what PIO will send; an answer aimed at the wrong run refused `not_found`, with `--json` and without, and one fenced at a stale epoch (after `client claim`) refused `stale_authority_epoch`, each exit 3 with the service's error object verbatim, both runs still waiting and nothing answered; then a real answer, on the stream as the caller's; the same two requests on a service that keeps only its last three events, where the walk says it has a gap and still lists both, from the runs' views, marked lost to retention with no invented deadline; a cancel acknowledged and the run exited; a steer refused `unsupported_required_feature`. On each of `serve-opencode`, `serve-claude` and `serve-codex`: `client answer … allow`, with the stream's decision the caller's `allow` (Codex `accept`) and the fake's own marker showing what it received (OpenCode `opt_1`/`allow_once`, Claude `allow` with no widening field, Codex `{"decision":"accept"}`). `respond_action` needed no service change: the epoch fence applies to every execution command.

Its mutants, each killed at its named assertion. Four the script plays: `cross-run` (the wrong-run answer aimed at its own run is taken), `fresh-epoch` (the stale answer sent at the current epoch is taken), `forgot-last` (a watcher that kept only the page cursor prints events 1–5 twice), `saved-page-end` (one that saved the page's end early loses events from 6). Five in the source, `pio client` built from a worktree of HEAD with one edit while the services run from the checkout: `human-refusal-exits-0` (the review's M3), `answer-always-deny` (M4: the stream shows `deny`), `approvals-gap-blind` (`[] != ['run-1.action-1', 'run-2.action-1']`), `ledger-exits-0` and `watch-ignores-stream` (exit 3, `invalid_cursor`).

The caller ledger behind `client submit` and `client reconcile` moved from `pio_protocol::client` to `pio-cli` and reaches the service through `pio_client`; it records what it did before, and `client_recovery.py` passes with `--json`, asserting exit 4 where the service answered `internal_error` and where a reconcile could not establish the submission. The `client` job in CI runs all of the above apart from the runtime job, whose `cargo test` still runs pio-client's own tests.
