# Current session state — PIO

Dated snapshot; reconcile Git with [M3 issue #7](https://github.com/Combraton/pio/issues/7) and the [M3 task packet](m3/TASK.md). Issues own live progress.

- **Updated:** 2026-09-21. **Owner:** fresh builder session for M3 (assigned by the owner). Independent review and owner acceptance remain separate.
- **Task:** M3 — Claude Code adapter with real journeys. Issue [#7](https://github.com/Combraton/pio/issues/7). Start from the [kickoff prompt](m3/KICKOFF.md).
- **Branch/base:** `codex/m3-claude-code` in the `pio-m3` worktree, based on **`16fb22912f93…`**, the merge of [PR #6](https://github.com/Combraton/pio/pull/6). Its tree is byte-identical to the reviewed head `699bf6f`. Worktrees `pio`, `pio-m1` and `pio-m2` are preserved; never modify a sibling.

## M2 is merged and closed

**M2 delivered the first real harness adapter.** PIO drives the owner's installed Codex 0.155.1 app-server behind the durable host: qualification by wrapper, Node and native binary hash against a 312-file canonical schema identity; content-addressed briefs; native approvals as Protocol actions; steering, interrupt, usage, workspaces and observed-only discovery; a thread-settings guard that refuses anything broader than the user's configured default; and the ADR 002 capacity bound with admission headroom. Issue #5 closed; [PR #6](https://github.com/Combraton/pio/pull/6) merged as `16fb2291` after three reviews.

- **Live evidence:** R1 on the user's configured model with nothing passed, R2–R6 on an explicit model under a dated test-only exception, plus zero-token `model-list`, `discovery` and `wrong-executable` runs. **421,450 observed tokens** of the 1,000,000 Codex cap. Receipts in [codex-live](m2/codex-live/); the [acceptance packet](m2/ACCEPTANCE.md) marks J1, J3, J4 and J5 as **partial acceptances** with their obligations, and J2 and J6 `not_evaluated`.
- **Named obligations carried into M3 and beyond:** incremental changed-key commits before M3b or M4; **product budget enforcement is unproven** — the M2 stops were the live runner's, not PIO's; `test_only_model_exception` must be removed or compiled out before release; J5's negative control does not exist; the exact-version pin refuses every Codex that is not 0.155.1, which is right for M2 and wrong for real users.
- **Upstream:** [Protocol #13](https://github.com/Combraton/protocol/issues/13) capacity error, [#14](https://github.com/Combraton/protocol/issues/14) content path for digest-only fields, [#15](https://github.com/Combraton/protocol/issues/15) reattach recovery decision. All open; none widened locally.

## M3 complete — Claude Code, with M3b offline-only

**The Claude live sequence is closed.** Eleven turns, charged 670,500 of the 1,000,000 cap and observed 590,500; the 800,000 stop was never reached. The [M3 acceptance packet](m3/ACCEPTANCE.md) records four partial acceptances from live evidence only, and [JOURNEYS](../JOURNEYS.md) now lists Codex and Claude Code side by side. Receipts in [claude-live](m3/claude-live/).

What the live runs actually found was five defects in **PIO**, not in the harness, each hidden the same way — a field that was present and therefore looked checked:

- a transcript listing keyed by the directory fixtures are created in, so a run that wrote 194 KB reported that it had written nothing;
- two runs named for a decision the runner could not send, and a third for a cancel it never sent;
- an **empty** usage block counted as an observation of zero, with the stop rule reading `if not reported`;
- **PIO never attached as the host that answers permission prompts**: it passed `--permission-prompts host`, named no prompt tool and sent no handshake, so the CLI denied anything that would prompt and PIO never learned it was asked;
- refusals attributed to the harness when the caller had just decided them — a defect created by the fix for the one above it.

All are fixed, mutation-checked, and disclosed in dated addenda on the receipts rather than by rewriting them.

**M3b is offline-only and held.** The [OpenCode field audit](https://github.com/Combraton/pio/issues/10) found eighteen receipt fields null or constant, because the receipt builder was copied from the Claude one and never adapted to ACP; the model evidence the owner's rule turns on was measured by the host and thrown away. All six audit fixes are in, with a runner self-test that fails if any receipt field is null or unchanged across two scenarios. **No MiniMax run has happened**, and the first live `session/request_permission` will be the first measurement of a shape taken from the ACP specification.

## M3, the work behind it

Branch `codex/m3-claude-code`, draft [PR #9](https://github.com/Combraton/pio/pull/9), CI green at `e2792e0` on both platforms. Five reviewer reviews so far; every correction is closed.

**Landed:** [ADR 004](../decisions/004-claude-code-adapter.md) choosing the executable's own structured I/O over the SDK bridge; `pio-claude` qualification by **command-line surface identity and a pinned stream identity**, because a help digest cannot see the wire; the credential route observed and never read; an equality-only permission-mode guard; the labeled fake `pio claude fake-cli`; the pre-spawn admission decisions; an eleven-case offline matrix; and `pio_host::claude`, the first harness written on the **shared host lifecycle** extracted in `53b37c6`.

**Measured at zero tokens, and each changed the design:** `system/init` does not arrive until a message is written to stdin, so the effective mode cannot be checked before the brief is released; the product's default model is Opus even with an empty configuration, so a receipt showing Opus is no evidence the user's settings were honoured; containment is the permission rules only, because the user has no sandbox key and 38 Bash allow rules across two settings files, so a pre-approved command never reaches PIO at all.

**Two defects found in committed code and fixed:** `auth-route` refused a route that works, because it passed no `USER` and pointed `CLAUDE_CONFIG_DIR` at a directory the product then could not read; and fixture placement was a lexical prefix test that called a traversal contained and a relative path outside.

**The service binding is in.** `pio serve-claude` admits a `pio-claude-service/1` configuration, refuses to start when admission refuses, and launches `pio claude host` through the same `submit_harness` path, launch guard and host-slot fence Codex uses. A turn through the service records `delivery: acknowledged` with evidence class `native_replay_echo` and proof class `provider_ack_id`, the containment statement, and an observed `claude.tokens.total`. The provider layer is now adapter-generic with a **per-harness event codec**, so OpenCode gets the third adapter without a third copy. [SERVICE-BINDING.md](m3/SERVICE-BINDING.md) records the analysis that shaped it. Restart, reattach and host-loss are now covered as they are for Codex.

**M3b** is open as [issue #10](https://github.com/Combraton/pio/issues/10) with [ADR 005](../decisions/005-opencode-adapter.md), pulled forward for OpenCode at the owner's request. Qualification, the labeled fake, the offline matrix (12 cases, two through `serve-opencode`) and the durable host are in; the MiniMax live plan is next and waits for the owner's go. Transport is the ACP stdio server, chosen because isolation is structural: `opencode acp` starts its own private server as a child and never touches the owner's running service, which every probe records by pid and start time. Two hazards are recorded rather than worked around: the environment does **not** isolate OpenCode's configuration, and a missing route does not refuse but silently downgrades to a free model. Two owner decisions are open on #10: which MiniMax model for fixtures, and whether the Juspay Grid gateway is approved for an as-configured run.

## Owner decisions still binding (recorded at M2 start)

Full text: [ADR 001 amendment](../decisions/001-standalone-stack.md#amendment--owner-decisions-after-m1-acceptance-2026-09-16) and [PLAN amendment](standalone-0.1/PLAN.md#owner-amendment-after-m1-acceptance-2026-09-16).

- PIO drives each harness **as the user installed and configured it**; it never selects or injects a model or provider. A same-day kickoff draft routing MiniMax through a Codex custom provider was **withdrawn by the owner**; nothing was configured for it.
- Test scope: Codex **0.157.0** (re-pinned from 0.155.1 on 2026-09-25, owner decision: the owner's Codex updated itself; evidence in [`m2/codex-qualification/`](m2/codex-qualification/README.md)) (M2 ran on 0.155.1, re-pinned from 0.146.0 on 2026-09-19), **Claude Code as installed** (M3; the readiness record's 2.1.273 is stale — M3 ran on 2.1.278, from a Homebrew cask that tracks latest and self-updates, so qualify what is there and expect drift; **re-pinned to 2.1.281 on 2026-09-26** at zero tokens, fully isolated, with the drift recorded in [`m3/claude-qualification/`](m3/claude-qualification/README.md)), OpenCode **v2.0.11** as `opencode2` (PLAN row M3b, after M3, confirmed; **re-pinned from 2.0.1 on 2026-09-21** after npm self-updated the install — qualification refused it, which is the refusal working). OpenCode preferred for heavy-usage testing. **v0.1 release scope remains Codex and Claude Code**; OpenCode and Hermes do not gate v0.1 unless the owner promotes them. Test scope is not release scope. **Hermes Agent v0.20.1 is deferred** (owner decision, 2026-09-21): test scope only, does not gate v0.1, and no adapter work happens on it unless the owner says otherwise.
- Codex live runs use the user's real Codex home with before/after configuration capture on every run, in a throwaway fixture repository path; never the full-access sandbox or `thread/shellCommand`; every added trusted-project entry disclosed.
- Hermes: **deferred** (owner decision, 2026-09-21), test scope only and not gating v0.1. If it is ever picked up, only under an isolated profile carrying model configuration; the owner's real Hermes home runs scheduled jobs and is never driven by PIO.
- Claude Code (M3): user's own login, or API key when configured; Anthropic caveat visible; route evidence, precedence rule and missing-route refusal.
- MIT license; M4 reviewer mockup design step; M6 reviewer walkthrough; thresholds unchanged.
- Reserved: evaluation thresholds/rubric, merge, tag, publication.

## Owner decisions of 2026-09-26

- **Codex's unmetered features for L3** (answering "Q6: ... how should they be turned off?", chose "Per-launch override"): "L3's Codex threads are launched with these five off, per launch, under a dated test-only exception like the lead-tool pre-allowance. Your ~/.codex/config.toml is not changed." Recorded as `owner-2026-09-26-l3-codex-unmetered-features-off`, the one entry in pio-protocol's `FEATURES_OFF_DECISIONS`; it covers exactly the five (sub-agents by its three keys, memories, goals, standalone web search, image generation, as `pio_codex::features_off` lists them), and `lead_run.py` sends it for L3's threads only. The live preflight accepts that route for each feature and refuses any feature the set does not cover; the receipt records each feature's route.
- **The owner's plugins, apps and MCP servers for L3** (answering Q7, chose "All off for L3"): "Per launch, under a dated test-only exception, L3's threads get features.plugins=false, features.apps=false, and your [three named MCP servers, names redacted] off. Only PIO's lead tool remains. Your config.toml is untouched." Recorded as `owner-2026-09-26-l3-plugins-apps-servers-off`, the one entry in pio-protocol's `PLUGINS_OFF_DECISIONS`, gated as `FEATURES_OFF_DECISIONS` is (a real Codex refuses any other decision; only the labeled fake takes the rehearsal's token). `lead_run.py` sends it for L3's threads only, lead and children: `features.plugins`, `features.apps` and its legacy alias `features.connectors` false (`pio_codex::plugins_off`), and every MCP server the owner's `config.toml` names by a table header as `mcp_servers.<name>.enabled = false`. The servers are found from `[mcp_servers.<name>]` header lines alone; read that way on 2026-09-26 there are four, and all four are turned off per launch. The owner named three because the fourth is already disabled in the owner's own configuration (the owner's word, 2026-09-26; the runner reads only header lines and does not see that setting), which is consistent with "Only PIO's lead tool remains". The receipt carries them only as sha256 digests and a count. Without the decision the live runner refuses (`plugins-decision-absent`).
- **Each L3 child's ceiling for the second attempt** (answering Q9, chose "Child 90k"): "Child ceiling 90,000; worst case 510,000. L3 sequence cap 685,000 / stop 548,000 (36,126 + 510,000 = 546,126). Codex cap 1,210,000 / stop 968,000 (457,576 + 510,000 = 967,576). Expected spend is unchanged (~150-250k); only the worst-case reservation grows." Asked because, read from Codex's source, `alpha`'s 30-second command outlasts both of code mode's yields and so takes at least one more model step than the rehearsal plays. The lead stays at 150,000 and a step in flight at 30,000; the shares are 210,000 and 150,000 and the worst case 510,000. In the code: `CODEX` in `scripts/lead_run.py` and `CAP`, `STOP_AT` in `scripts/codex_live_run.py`; `lead_run.py --sizing-selftest` checks that `--attempt L3-attempt-2` fits both stops from 36,126 and 457,576, that the sizing before Q9 refused it, and that a third attempt is refused once the second has charged more than 423. It supersedes Q8's figures, below. The Claude row is unchanged.
- **L3's sizing for its second attempt** (answering Q8, chose "Raise for attempt 2"; superseded by Q9 the same day): "L3 sequence cap 610,000, stop 488,000 (36,126 + 450,000 = 486,126). Codex cap 1,135,000, stop 908,000 (457,576 + 450,000 = 907,576). Limits per run unchanged." The lead stays at 150,000, each child at 60,000 and a step in flight at 30,000, so the worst case is still 450,000. In the code: `CODEX` in `scripts/lead_run.py` and `CAP`, `STOP_AT` in `scripts/codex_live_run.py`; `lead_run.py --sizing-selftest` checks that `--attempt L3-attempt-2` fits both stops from 36,126 and 457,576, that the sizing before Q8 refused it, and that a third attempt is refused once the second has charged more than 423. The Claude row is unchanged.
- **L3's sizing** (answering "Q5: L3's honest worst case is 405,000...", chose "More headroom"): lead 150,000, child 60,000, a step in flight 30,000, so the worst case is 450,000; the L3 sequence (`M4-lead-codex`) cap 565,000, stop 452,000; the Codex cap raised from 1,000,000 to 1,090,000 and its stop from 800,000 to 872,000. The Claude row is unchanged. In the code: `CODEX` in `scripts/lead_run.py` and `CAP`, `STOP_AT` in `scripts/codex_live_run.py`.
- **Live runs within caps are delegated to the orchestrator** (owner, 2026-09-26): each runs once gates from a fresh clone of its exact head pass and CI is green on that head, with the orchestrator's dated go line in the receipt. While independent verification is paused (next item), no independent verifier clears a live run beforehand; once verification resumes at the TUI phase, one does.
- **J6 is deferred.** J6 (real CBR composition, M5) is deferred past v0.1 and listed as a v0.1 limitation. v0.1's required journeys are J1–J5 and J7 (lead and relay truthfully) on Codex and Claude Code, on macOS arm64.
- **The full UI is in v0.1.** Every screen in the accepted design input: board, run view, approval walk, uncertain run, leaving and returning, split view, skins, mouse levels, notifications, the playful busy line, and the orchestrate screen. No tier is cut.
- **Independent verification is paused until the TUI phase starts.** Until then, PRs merge on gates run from a fresh clone of the exact head, plus CI green on that head. Live runs within caps go ahead on the rehearsal record plus the orchestrator's own checks, with no independent verifier. Receipts from this period must say they were not independently verified. When TUI work begins, independent verification resumes and reviews everything, including L3 and L2; from then on it includes live re-runs of each acceptance run. This amends the earlier rule that an independent verifier clears every PR before merge.
- **L2 and L3 attempt 2 are parked** (owner, 2026-09-26): "L2 and L3 attempt 2 live runs parked; product defects next, then the TUI immediately; J7 acceptance replaces the headless lead runs." L3 attempt 2 is built and rehearsed, with Q7, Q8 and Q9 applied, and its code is kept correct so that it could run if it is ever resumed; no live lead run is planned. J7 is to be accepted through the TUI from the installed package instead.
- **Lean mode** (owner, 2026-09-26): "lean mode: CI green on the exact head is the merge gate; local full gates run only before a live run; agent models are tiered." It amends the item above: a PR no longer needs gates run from a fresh clone before it merges, only CI green on its exact head; the full local gates are for the head a live run starts from.

## Live-run spend ledger

Counted from each harness's own usage reports; missing usage is unknown liability, not zero. Stop and report at 80 percent. Codex and Claude Code caps are separate, per harness (owner confirmation 2026-09-16); the three-session limit was waived for the M3 runs (owner, 2026-09-20).

The Claude ledger carries two numbers per run (owner decision, 2026-09-20). **Observed** is what the harness reported; **charged** is what the cap is measured against, and **every stop rule uses charged**. They differ only for the two cancelled turns, which reported an empty usage block: each is charged a flat **40,000**, the basis being the only two single-call turns measured, at 33,793 and 32,957.

| Cap | Limit (tokens) | Stop at | Used | Runs |
| --- | ---: | ---: | ---: | ---: |
| Codex, all tests (owner, 2026-09-26: **raised to 1,210,000, stop 968,000**, for L3's second attempt with each child at 90,000, Q9 "Child 90k": "Codex cap 1,210,000 / stop 968,000 (457,576 + 510,000 = 967,576)"; the same day it was raised to 1,135,000, stop 908,000, Q8, and before that to 1,090,000, stop 872,000, "More headroom"; the Claude row is unchanged) | 1,210,000 | 968,000 | **457,576** (L3 attempt 1, 36,126, on 2026-09-26) | 8 |
| Claude Code, all tests (owner, 2026-09-24: **raised to 1,400,000, stop 1,120,000**, for L2; the ledger changes when L2 is built) | 1,000,000 | 800,000 | **670,500 charged**, 590,500 observed | 11 |
| MiniMax via OpenCode (owner, 2026-09-24: cap and stop unchanged. Review 47 relayed "MiniMax usage unlimited"; asked in the builder session, the owner kept 300M with the stop at 240M) | 300,000,000 | 240,000,000 | **372,095 charged**: M3b 86,863 (62,220 as recorded; corrected 2026-09-23), L1 73,532 and L1b 128,790 (2026-09-24), L1b attempt 2 82,910 (2026-09-25); 135,536 observed | 16 |
| Lead sequences (owner, 2026-09-24), each inside its harness's row: `M4-lead-opencode` (L1, L1b) 2,000,000; L3 (Codex, `M4-lead-codex`) **685,000, stop 548,000** (owner, 2026-09-26, Q9 "Child 90k": "L3 sequence cap 685,000 / stop 548,000 (36,126 + 510,000 = 546,126)"; the same day it was 610,000, stop 488,000, Q8, and 565,000, stop 452,000, "More headroom"; 400,000, stop 320,000, before); L2 (Claude) **500,000** | per sequence | 80 % | `M4-lead-opencode` 285,232 (L1 73,532, L1b 128,790, L1b attempt 2 82,910): **closed** (owner, 2026-09-25); no further OpenCode lead run without a new plan; `M4-lead-codex` 36,126 (L3 attempt 1, failed: the lead never saw its tool). A second attempt fits under the owner's Q9 sizing: 36,126 + 510,000 = 546,126 within the 548,000 stop, and 457,576 + 510,000 = 967,576 under the 968,000 Codex stop. A third is refused once the second has charged more than 423. Attempt 2 is parked (owner, 2026-09-26) | 9 |
| GLM/Kimi via OpenCode — **proxy-counted** in the MiniMax row until the owner sets per-provider caps (required before M3b) | proxy | — | 0 | 0 |

Two runs are kept under their own names rather than replaced, because their tokens were spent: `R5-attempt-1`, which cancelled nothing, and `R5-attempt-2`, whose cancelled turn PIO counted as zero. Two are recorded as **not run** with their reasons: `R4`, because no permission request arrives for its brief, and `R6b`, because the charged total would not have stayed under the owner's 700,000 threshold afterwards.

## Accepted baseline and current counts

- **M1** owner-accepted at `2048e84` and merged as `9cf7047`: journal-backed Core/Execution slice, public durable labeled fake-process host, caller operation ledger, content-addressed output, recovery and order fences, truthful fake discovery, packaging skeleton. [Acceptance corrections](m1/ACCEPTANCE-CORRECTIONS.md), [CHECKPOINTS](m1/CHECKPOINTS.md).
- **M2** merged as `16fb2291`. Current counts at that head: **46 Cargo tests + 1 ignored probe**; offline Codex matrix **17 cases × 3 = 51**; public matrix **72** (51/6/9/6); diagnostic matrix **54** (39/3/9/3); caller recovery; pinned runner **206 pass / 73 unsupported / 1 skipped** plus two supplemental passes counted separately. Three repetitions per case. Green on macOS 15 arm64 and Ubuntu 24.04 x86_64.
- **M3/M3b current counts:** **100 Cargo tests + 1 ignored probe**; Claude matrix **26 × 3 = 78**, fourteen through `serve-claude`; OpenCode matrix **25 × 3 = 75**, twelve through `serve-opencode`, plus the OpenCode runner self-test and the private-path gate; Codex matrix, public matrix and diagnostic matrix unchanged at 51, 72 and 54, which is the proof the shared work changed nothing else.
- **What is still not established:** no TUI, no CBR composition, no packaged-install journey, no Claude Code adapter, no throughput claim, no total-disk or spool-GC bound, no universal exactly-once property, and **no product budget enforcement**. Process cancellation, workspace and usage adapters are unavailable in the fake process mode. Protocol #9, #10, #13, #14, #15 and the undeclared subscription authorization-recheck barrier remain coverage limits.

## M2 history

The narrative of M2 — the capacity-bound checkpoint, Codex qualification, the offline adapter, three independent-reviewer reviews, the re-pin to Codex 0.155.1 and the six live runs — is recorded where it belongs rather than repeated here: the [acceptance packet](m2/ACCEPTANCE.md), the [capacity-bound report](m2/COMMIT-BOUND.md), [ADR 003](../decisions/003-codex-app-server-adapter.md), the [qualification evidence](m2/codex-qualification/README.md), the [live receipts](m2/codex-live/) and [issue #5](https://github.com/Combraton/pio/issues/5), which carries every receipt as it landed. Some of those M2 records say "owner review" where the review was the independent reviewer's, relaying owner decisions; they are left as written rather than rewritten after the fact.

## Resources and next action

- **Active resources:** worktrees `pio`, `pio-m1`, `pio-m2`, `pio-m3` and `pio-m4-drawn` under `Combraton/`, and a scratch directory beside them at `Combraton/pio-scratch` for rehearsal output. After the M2 and M3 runs, no PIO daemon, host, user job or live harness session was left running (process table inspected). Private live evidence lives under `$HOME/pio-m2-live` and `$HOME/pio-m3-live` at mode 0700; matrix stores, rehearsals and CI artifacts stay outside Git. **Scratch paths must stay short:** a Unix socket path is capped near 104 bytes, and both live runners now refuse a run whose socket would exceed it rather than failing as a service that never becomes ready.
- **M3b is merged** as `a22c98c` (PR #11). Seven live MiniMax turns on OpenCode 2.0.11, **62,220 charged / 50,220 observed** of 300,000,000 as recorded. **Corrected to 86,863 charged** (2026-09-23): OpenCode reports only a turn's last model step, and R2, R3 and R5 each took two, so their first steps (24,643 together) were missing. The correction is in the [M4 map](m4/SCREEN-INTERFACE-MAP.md) under L1, review 44. Two partial acceptances (J1, J4), J3 and J5 `not_evaluated` for this harness, seven open obligations: the [M3b packet](m3b/ACCEPTANCE.md). The permission finding is the one to carry forward: **this harness asks about some operations and not others** — a read outside the workspace prompts and PIO declines it; a shell command inside it does not prompt and simply runs, under either posture.
- **M4 steps 2 and 3 are merged** as `d0fd88f` ([PR #15](https://github.com/Combraton/pio/pull/15), 2026-09-23), against the design the owner accepted on 2026-09-21, copied to [`m4/design-input/`](m4/design-input/M4-DESIGN-INPUT.md). What is proven and what is not is in the [screen-to-interface map](m4/SCREEN-INTERFACE-MAP.md) and on [issue #12](https://github.com/Combraton/pio/issues/12): the board fold (G1, G6), the approval walk (G2) and transcript blocks (G3) through all three release harnesses; `origin` on submit and the lead grant; the zero-token lead-tool probe. **Nothing is promoted into the inspect view** (`unevaluatedProperties: false`): every extra field rides in an open event payload under a namespaced key, and conformance stays at **206**. **Next: L1**, the first live lead run on OpenCode / `minimax-coding-plan`, on branch `codex/m4-l1`. It is rebuilt and rehearsed before any token is spent, because the first rehearsal's lead was never admitted and its live path did not exist (reverted in `f915137`). Then L2 and L3, the core screens, M4a.
- **In parallel:** the drawn-tier Canvas experiment, time-boxed to one day, in its own worktree `Combraton/pio-m4-drawn` on `codex/m4-drawn-tier` pinned to `a22c98c`. It must never block M4; if it is not clearly good the character tier ships alone.
- **L1 ran live** on 2026-09-24 at `703706d` and is **merged** as `655344e` ([PR #16](https://github.com/Combraton/pio/pull/16), 2026-09-24, after review 47): every row holds (two of them vacuously: no session existed to delete, and no approval was asked), **73,532 charged**, measured from the store's recorded steps. The receipt and what it does not show: [`m4/evidence/L1-live.json`](m4/evidence/README.md). Review 46's corrections landed before the merge. **Next, before L3** (review 47): the Codex host writes `config_after` before its exit event, and the matrix records the line that failed; the L2 and L3 runners judge a row whose observation could not have failed as inconclusive. The L2 and L3 plans are [posted](https://github.com/Combraton/pio/issues/12#issuecomment-5814304050); the owner approved L3 and, with the Claude cap raised, L2, and set the order **L1b, then L3, then L2** (2026-09-24). **L1b ran live and failed on the desk** (2026-09-24, 18:11:55–18:19:35Z, at `990c985`, on battery): OpenCode asked about `beta.env` live (`inside_fixture`), the request was relayed at once, and the owner's answer arrived after the 300-second deadline, so the host's single-use reject landed and `beta` could not count. The lead's read waited 54.6 s. 128,790 charged. No retry without the owner. Receipt: [`m4/evidence/L1b-live.json`](m4/evidence/README.md). **L1b attempt 2 ran live and held** (2026-09-25, 04:59:36–05:01:11Z, at `0ba60de`, on AC power, approved by the owner after review 50, go in review 51): 32 of 33 rows hold, and "PIO deleted no session" is inconclusive (nothing existed to delete). `beta`'s `.env` read was allowed by the relay from the owner's advance decision, not answered live, within a second of reaching the desk. The lead's read of `alpha` waited 53.1 s and returned `exited`, and both counts were relayed true. 82,910 charged; the sequence stands at 285,232 of its 1,600,000 stop, and there is no attempt 3 without the owner. Receipt and relay source: [`m4/evidence/L1b-attempt-2-live.json`](m4/evidence/README.md). L1b's plan ([#12](https://github.com/Combraton/pio/issues/12#issuecomment-5814416148)) had been **approved on 2026-09-24 with review 48's amendments**: a placeholder `.env` read for the desk, and the waiting read measured from the tool log. For L2, the owner's "allow the calls" means exactly this: the L2 lead **only** is launched with `mcp__pio-lead__start_run` and `mcp__pio-lead__read_run` pre-allowed, passed per launch and never written to the owner's Claude settings. No bypass, skip-permissions or yolo mode. Every other permission, for every run, comes to the owner's desk, and the answer is never "always". For L3: Codex's reported total covers every step (review 48, from M2's host events), so the charge is that total. The lead's limit is sized for five steps taken one at a time at about 22,000–24,500 each, and the worst case is rechecked against the 320,000 stop. If it doesn't fit, the numbers go to the owner before any build. L2 gets the same treatment at about 32,500 a step.
- **L1b is complete** (owner decision, 2026-09-25, after review 52): attempt 2 held, receipt `d0736b8`. The `M4-lead-opencode` sequence is **closed at 285,232**; no further OpenCode lead run without a new plan. **OpenCode's helpers (review 52; a static reading of 2.0.11, not measured):** the title and compaction helpers use the session's own provider and model. A `small_model` setting, or a model override on a helper, would change that. **Owner decision, 2026-09-25:** before any future OpenCode live run, the runner refuses to start if the owner's configuration points `small_model` or any helper at a provider other than the plan's. It reads `small_model` and the `model` of every `agent` and `mode` entry, from `opencode.json` and `opencode.jsonc`, before anything is reserved; mutant `helper-elsewhere` plants one in a rehearsal's own configuration. **D18 (2026-09-26):** this rule is no longer only the rehearsal runner's. `pio_opencode::service_admission` now refuses `helper_elsewhere` the same way, so every `serve-opencode` admission gets it, not only one driven through `scripts/lead_run.py`.
- **L3 ran live and failed** (2026-09-26, 08:54:14–08:55:48Z, at `8be3803`, on AC power; the orchestrator's dated go under the owner's delegation; not independently verified, as verification is paused until the TUI phase). **The lead never saw its tool.** Codex started `pio-lead` and listed its two tools, but `gpt-5.6-terra` runs in code-mode-only at 0.157.0: the model sees only `exec` and `wait`, and every ordinary MCP tool is deferred behind them and never named (read from Codex's source, see the receipt's README section). The lead said so, spent two steps and started nothing, so every child row fails. Charged **36,126** from Codex's own total; every spend, containment and configuration row held. Its one tool call went to a tool from the owner's Codex plugins that Codex auto-approves as read-only; the fix for the next attempt is PIO's own server setting `omit_tools_from`, and the plugins question goes to the owner. Receipt: [`m4/evidence/L3-live.json`](m4/evidence/README.md).
- **L3 attempt 2 is prepared** (2026-09-26; not independently verified, as verification is paused until the TUI phase). After attempt 1: the lead's tool goes in the model's own list (`omit_tools_from = ["code_mode","deferred"]`, `required`, a 30-second start, the host waiting for it to be ready before the lead's turn), the owner's plugins, apps and MCP servers are off on every L3 thread per launch (Q7), the children's code-mode commands are played in rust-v0.157.0's shape (read from source: under `on-request` in a `workspace-write` sandbox Codex does not ask about them, so the desk row is expected inconclusive), the lead's brief names its tools as the model sees them, the memory row takes a time that moved alone as inconclusive, and the sizing is the owner's Q9 (each child 90,000, worst case 510,000; sequence cap 685,000, stop 548,000; Codex cap 1,210,000, stop 968,000). Local gates of both CI jobs passed at `81174a1` (the Codex matrix again at `3798caa` after a test fix); for Q9, in lean mode, the targeted checks passed at `f9d54a7` (tests, clippy, the sizing and charge self-tests, the L3 rehearsal and the 23 mutants sized to a child's ceiling or share, each dying on its row); the rehearsal record at `84a227b`: 49 rows and 3 records, 46 holding and three inconclusive against the fake ([evidence README](m4/evidence/README.md), `L3-rehearsal.json`). The dry pre-check passes `--attempt L3-attempt-2` from 36,126 and 457,576. **Parked** (owner, 2026-09-26): no live run follows; product defects are next, then the TUI, and J7 acceptance replaces the headless lead runs.
- **L3 is built** (2026-09-25; owner approval of 2026-09-24 and decisions of 2026-09-25), with **no tokens spent**. It is `lead_run.py --plan L3` on Codex **0.157.0** (re-pinned) and `gpt-5.6-terra`: one lead, budget 2, and two children, each running one command (`sleep N && wc -l`). Limits (the owner's): lead 125,000 and each child 50,000, a step at most 30,000, sequence cap 400,000 and stop 320,000. **The review of L3 found the per-run shares were not bounds**: Codex reports a step once its tool has finished (M2 R5, R6), so a report that crosses a ceiling arrives with the next step begun. The second review found that a result the lead's tool never sees (Codex's own shell, another MCP server, a refused approval) starts a step before anything can stop it, so the lead too can reach its ceiling plus two steps. The worst case computed from the code is **405,000** (lead 185,000 + two children at 110,000), past the 320,000 stop and the 400,000 sequence cap, so the live runner refuses to start L3 until the owner decides the sizing (the Codex cap check refuses it too: 421,450 + 405,000 = 826,450, past 800,000). The lead's tool gate (every response withheld once the lead has reported more than 95,000) and the meter's stop on a result the tool never saw keep a lead that uses only its tool within 155,000. A run that ended by itself is charged Codex's reported total, the sum over its threads; one whose turn was cut short (the runner's stop, or an interrupt the host sent) that total plus a step in flight on each of its threads (30,000, or its largest step, if larger), not capped; one not seen exited, or stopped for silence, its share or that, if more; the [evidence README](m4/evidence/README.md) has every case. The lead tool rides on the lead's own thread config, with its two tools pre-allowed there (owner decision). MCP tool-call approvals come to the desk (a single `accept`, never `persist`), and an unanswered approval lapses to one decline of PIO's (owner decision). Every thread's model, provider and `approvalsReviewer: user` are checked from Codex's own answer before its first turn. When Codex asks before an MCP tool call is read from its source at both tags, not measured. The live desk's relay (`scripts/l3_desk_relay.py`, owner decision, item 3) answers only the two children's exact commands and, should Codex ask despite the pre-allowance, the lead's own two tools; everything else goes to the owner live. It is rehearsed against a desk that waits for answer files (`--relay`). **The review of L3** (three lenses, 2026-09-25) found, among others, that requests PIO declines by itself never reached the receipt, that the shares were not bounds, that a preamble could fail a correct count, and that a service which never started left every share reserved; each is fixed with a mutant. **A second, independent review** (2026-09-26) found that the lead's tool gated its results but not its errors, and that a result the tool never sees could still carry the lead past its share; that a run not seen to exit could be charged less than it spent; that a symlink before the last component of a path was followed out of order, so a path through it could be placed inside the fixture when it was not (a defect in `pio-claude` since M3, which Claude placement shared); that the Codex qualification was compared only after the shares were reserved; and that a request arriving before the first turn was dropped unanswered. Each is fixed with a mutant; what it did not fix is listed under [Deferred after the L3 reviews](#deferred-after-the-l3-reviews). **A third review** (2026-09-26) found that a child started under another name escaped every meter, stop and charge; that **Codex 0.157.0 has sub-agents on by default** and a sub-agent's thread reports on PIO's connection, where the host took it for the run's own; that the feature preflight missed Codex's legacy alias `request_permissions`; that the one step size the bound rests on was never checked; that the path resolver still failed open on a `readlink` it could not read (past `PATH_MAX`), which was open in Claude and OpenCode placement since M3; and that the owner's `[features] memories = true` has Codex run a background memory pipeline that writes outside `config.toml` and makes model calls PIO never sees. Each is fixed with a mutant, or disclosed where it is Codex's to do. **Owner decision, 2026-09-26, L3's sizing** (answering "Q5: L3's honest worst case is 405,000", chose "More headroom"): lead 150,000, child 60,000, a step in flight 30,000, so each share is 210,000 and 120,000 and the worst case **450,000**, within the new L3 sequence stop (cap 565,000, stop 452,000); the Codex cap is raised from 1,000,000 to 1,090,000 and its stop from 800,000 to 872,000, so 421,450 used + 450,000 = 871,450 stays under it. The Claude row is unchanged. The live pre-check, run dry at the new head (build, preflight and ledger stubbed; the live tree an assertion), no longer refuses on sizing. **Owner decision, 2026-09-26, how Codex's unmetered features are off:** per launch, under the dated decision `owner-2026-09-26-l3-codex-unmetered-features-off`, which the L3 plan sends; see [the decisions of 2026-09-26](#owner-decisions-of-2026-09-26). Before those decisions: **two things stood before a live L3, both the owner's to decide:** the sizing (405,000 with sub-agents off, past every limit), and how sub-agents are off (the owner's own `[agents] enabled = false`, or a dated owner decision that turns them off per launch; `multi_agent = false` alone is not enough for `gpt-5.6-terra`; with them on the worst case is 675,000 for multi-agent V2 or 945,000 for V1). The runner refuses a live L3 until both are settled. Whether to turn the memory pipeline off for the run is the owner's call too. **A fourth review** (2026-09-26) found that Codex 0.157.0 has five unmetered, default-on features whose spend no report PIO reads covers (sub-agents, the owner's memory pipeline, goals, standalone web search, image generation), and that the runner knowingly started a live L3 with the memory pipeline on and its alias unread; that a goal's continuation turn ran after the host stopped reading the run's thread; that the charge could fall below the floor its own receipt computed, and an admitted probe was charged a flat share; that the relay's server check, a sub-agent interrupted with the run rather than after it, the resolver's `EACCES` and `ENOTDIR` rules and two MCP-recognition rules had no test; and that two fake shapes and a docstring overstated 0.157.0. Each is fixed with a mutant: one owner-gated override set for the five features (`pio_codex::features_off`) and a live preflight that resolves each off, from the owner's configuration or per launch, or refuses; the host reading the run's own thread for three seconds after its turn and interrupting a continuation; the charge read from the host's files as the floor is; probes charged as runs; willRetry recorded and retries counted per run. What still spends outside the bound is listed in `BOUND.assumes` with its source: remote compaction (unreachable at the catalog's limit), the startup prewarm request, and stream retries. The rehearsal record after four rounds and the owner's decisions of 2026-09-26 ([evidence README](m4/evidence/README.md), `L3-rehearsal.json`): 44 rows and three records at `fff09be`, 41 holding and three inconclusive against the fake, the five features off per launch under the owner's decision, 450,000 reserved and 36,864 charged. Next: the owner decides, the reviewer reads the pushed head, and then the owner gives a dated go with the desk words.
- **The live runs still planned are the lead runs**, each on its own plan approved first: L1 on OpenCode / `minimax-coding-plan`, L2 on Claude Code / Sonnet, L3 on Codex / terra. Hermes stays deferred: test scope only, not gating v0.1.
- **Three adapters now share one lifecycle and one provider.** `pio_host::harness::Lifecycle` owns the fences, guard, spawn marker, park, release gate, event and control files, deadline stop and receipt; `pio_protocol::codex::PROFILES` holds the per-harness table — delivery evidence and its proof field, usage measure, decision vocabulary, cancel description, and the event names that could not be shared. OpenCode needed **no new provider code** beyond a table row.
- **What each harness can actually prove differs, and the receipts say so.** Codex returns a turn id, `provider_ack_id`. Claude Code echoes the message sent, which is evidence (`native_replay_echo`) but no identifier, so since 2026-09-26 (D7) it carries **no proof class**; M3's receipts recorded `provider_ack_id` before that and are not rewritten. **OpenCode acknowledges nothing**, so its delivery carries evidence class `native_session_update` and **no proof class**, and more of its outcomes will stay ambiguous. Conversely OpenCode's provider-and-model refusal **precedes delivery**, which Claude Code's cannot, because `system/init` never arrives until the brief is already sent.
- **Before any M3 live run:** the ADR, qualification with its negative controls, the offline matrix against a labeled fake, the missing-credential-route refusal **and the service binding** must be committed with CI green, and the tree must be clean. A receipt from a dirty tree is not evidence.

## Product defects, Claude batch (2026-09-26, branch `codex/w3-claude-batch`)

Not independently verified: verification is paused until the TUI phase. Defect numbers are the matrix synthesis's (section 5).

- **D12 (Claude half): re-pinned to 2.1.281.** Measured at zero tokens with `claude_requalify.py --isolated`, so no child saw the owner's configuration and the owner's route on 2.1.281 is **not** observed. Drift from 2.1.278: the top-level help, two new `system/init` keys (`per_turn_effort_active`, `view_mode`), two new capabilities (`mcp_read_resource_v1`, `mcp_tool_ui_meta_v1`) and the product default model (`claude-opus-5-5[1m]`); the stream identity was measured at zero tokens, not carried over. An empty configuration now shows two plugins, so ADR 004 §8's "empty list" discriminator needs plugin names from here on.
- **D3: a use PIO denied is never `performed`.** Without a final `result` (a kill, a crash) the audit had no denial list and recorded PIO's own decline as `performed`, `denied: false`. Now a deny PIO sent, for itself or a caller, is a refusal with or without a `result`, and a use nobody denied is `unknown` with the liability unresolved (`pio-claude-tool-uses/6` adds `result_observed` and `unknown_outcome_count`). The OpenCode host shared the branch and passes whether its prompt answered. The Protocol's `effects_liability` now follows the record's own `liability`.
- **D2: an absent `permissions.defaultMode` is the product default.** Most installations configure none, and admission refused them all. The guard now compares the requested mode with the measured product default (`default`, from the pinned stream identity), records `configured_source`, and a run requesting it passes no `--permission-mode` flag (the flag does not accept the name); the `init` echo is compared as before. Equality only: nothing broader or narrower is admitted.
- **D7: Claude delivery earns no proof class.** The replay echo returns the message PIO sent and no identifier, so it is evidence class `native_replay_echo` with no proof class, the way OpenCode's `native_session_update` is. The rule is now one function, `codex::delivery_proof`: only an identifier string the harness returned (Codex's turn id) is a `provider_ack_id`. The profile table only gained a comment.
- **D11: the product holds each run to the pinned stream identity.** The first `system/init`'s key set and capability list are compared with the pinned identity; on drift the host records the observed and pinned digests, reads only the replay echo that follows, stops the child and refuses the run (`stream_identity_refused`, reason `stream_drift`) before any tool use is answered. The view then shows delivery acknowledged, exit unavailable and usage `unresolved`. The labeled fake now takes its `init` keys and capabilities from the pinned identity; its label key is the only extra a labeled fake may carry. The post-delivery permission-mode mismatch still failed through `host_error` (projected as a lost host) when this landed; it is refused the same way since `a44700c` (see [Approval and audit correctness](#approval-and-audit-correctness-2026-09-26-branch-codexw3-approval-correctness)).
- **G4, a deviation (decided by the orchestrator for v0.1): a Claude Code run is not steered mid-turn, and the steer is refused truthfully.** The decision allowed a message to a running Claude run to be accepted as **queued** for its next turn, if that was small. It is not: the Claude host runs one turn and closes stdin at its `result`, so a queued message would never run, and whether the harness steers, queues or drops a second mid-turn message is unmeasured (ADR 004 §10). So a steer to a Claude run is `request: not_supported` with the alternative `not_supported_mid_turn: …`, which is true, instead of the generic "needs an acknowledged, running native turn", which was false for a running Claude run. Nothing is queued, no control reaches the host, nothing is shown delivered. Queued delivery would need a multi-turn Claude host and a measured queue; it is not in v0.1. OpenCode's steer keeps the generic reason (D6, another branch). Steering is still advertised for every native adapter (also D6).
- **G5, per-message usage: not built, because no recorded shape carries it.** Checked, not assumed: the pinned stream identities (2.1.278 and 2.1.281) record message types and the `init` and `result` key sets, not an `assistant` message's shape, and the labeled fake's `assistant` messages carry no `usage` at all. ADR 004 says R1 *measured* a full `usage` block on `assistant` messages, but no committed receipt or identity records that shape. So the Claude host still reports usage once, from `result`, and a stop is still a next-turn stop. Building G5 needs that shape recorded first (a model turn, so not at zero tokens), and the fake taught it.

## Approval and audit correctness (2026-09-26, branch `codex/w3-approval-correctness`)

Not independently verified: verification is paused until the TUI phase. Offline only, against the labeled fakes; no live run. Each item has a test and a mutant that dies on it.

- **CH-7: one decision per Codex approval** (`cd9193c`). The lapse is decided by the service, where every answer is decided, for an action still pending strictly after the deadline the host advertised, and reaches the host as a control marked `decided_by: pio`; the host keeps no answer clock and sends whichever control for a request comes first. An answer after the lapse is refused `not_found` with `already_decided` and who decided; no lapse follows a caller's answer, and none is decided for a run whose runtime is `exited` or `unknown`. PIO's decision reaches the stream when the host reports it sent. The Claude and OpenCode hosts still lapse by themselves, so the same race remains there.
- **CH-8: a request Codex settles itself** (`3a0d0b9`). The host drops it from its pending requests on `serverRequest/resolved` and records `settled_by: harness`; the action's one decision is `decided_by: harness`, `sent: false`. Nothing is sent for it afterwards, not a lapse and not a caller's answer.
- **An approval pending when its turn ends** (`6b12b09`). The Codex host closes each one with `request_expired_with_turn`: `decided_by: nobody`, basis `expired_with_turn`, `sent: false`. An answer reaching the host after `turn/completed` is not sent, and `execution.respond_action` refuses an answer to a run whose runtime is `exited` or `unknown`.
- **D5: Codex usage liability** (`a6ce007`). A per-step report leaves it `unresolved`; the host's `usage_final`, sent only when the run's turn completed and nothing was cut short, resolves it. A run interrupted, failed or lost stays unresolved.
- **Claude: a permission-mode mismatch at `init`** (`a44700c`). Refused the way D11 refuses a drift: the echo alone is read, the child stopped before any tool use is answered, and the turn ends cleanly (`permission_mode_mismatch_refused`). The view reads delivery acknowledged, exit unavailable and usage `unresolved`, not a lost host, and both refusals carry their reason on `execution.exit.observed` under `pio.combraton.dev/refusal`.
- **Claude: a cancelled turn's tool use in flight** (`d97ef0f`). A turn cut short (an interrupt sent, or a `result` whose terminal reason is `aborted_streaming` or `interrupted`) audits a use with no completed `tool_result` as `unknown`, never `performed`, and the liability stays unresolved (`pio-claude-tool-uses/7`, `turn_interrupted`).
- Checks (lean mode, CI is the merge gate): workspace tests, clippy and fmt; the Codex matrix 48/48 and the Claude matrix 30/30 at one repetition; the approval desk on `serve-codex` and `serve-claude`; conformance 206 pass, 73 unsupported, 1 skipped, plus 2 PIO regressions.
- Where a caller's answer was already on the stream when Codex settled the request or the turn ended, the stream keeps that one decision and its response effect is failed with the reason (`settled_by_harness_before_sent`, `expired_with_turn_before_sent`); it is not reported twice.

## Deferred after the L3 reviews

Findings of the L3 reviews (three rounds) that are not fixed on `codex/m4-l3`, with what each means for the owner. None of them is reachable only through a live run the runner would allow today, except where it says so.

- CH-7, CH-8 and the approval pending at exit are fixed on `codex/w3-approval-correctness`; see [Approval and audit correctness](#approval-and-audit-correctness-2026-09-26-branch-codexw3-approval-correctness).
- **CH-9, what an MCP approval shows.** An MCP tool-call approval shows its server, Codex's message and whether `persist` is offered, but not the arguments being approved (`_meta.tool_params`: for `start_run`, which child and which brief). For the owner: should Codex ask before one of the lead's calls despite the pre-allowance, the desk decides by tool name without seeing the arguments.
- **CH-10, lead-tool admission tests.** No test covers `serve-claude` refusing a lead-tool extension, and the credential check on a lead-tool spec reads its arguments and environment values but not its name or command (the name becomes a Codex thread-configuration key). Both predate L3. For the owner: nothing in L3, whose spec the runner builds; a caller-written spec with a credential in its name or command would not be refused.
- **F10, the live-tree row.** "The live tree can be released" cannot fail on the live path: a tree that cannot be released stops the run before anything is reserved, and no receipt is written. For the owner: in a live receipt this row always holds and says nothing; the release at the end of the run is judged by "The service was released, and nothing it started survived".
- **REPIN-6, the drift record's listing digest.** A drift record copies the expected listing digest from the expected identity's header instead of computing it from its files, so `drift-control-0.157.0.json` shows the same expected and actual listing digests beside `drift_count: 1`. It predates the re-pin (the 0.155.1 records have the same shape). For the owner: read `drift_count`, not the two digests, to tell whether anything drifted.
- **After round 4: what the preflight does not read.** The five features are resolved from the owner's `config.toml` and the per-launch override; a managed configuration (`requirements.toml`, MDM, cloud) or a profile that sets them is not read, and could override either route. The owner's `model_auto_compact_token_limit`, which could make remote compaction reachable, and `model_providers` retry settings are not read either. `[memories] generate_memories = false`, which would keep L3's own threads out of the owner's later memory extraction (in the owner's sessions, not L3's), is not in the decided set. For the owner: each would change what a live L3 spends only through a setting PIO does not look at.
- **R3-HC-8, placement is a snapshot.** PIO places a target when it classifies it: the Codex host a command approval's cwd when the request arrives, and the Claude and OpenCode hosts' end-of-turn audit every target after the turn, against the filesystem as it is then. A link changed in between (by a process still running in the run's sandbox, say) changes where the command runs, or where the audited target went, without PIO seeing it. It predates L3 (M3, for the audit). For the owner: a placement in a receipt is what was on disk when PIO looked, not a proof of where the command ran; the relay's inside-the-fixture rule inherits that.
- **V-6, ten red commits.** Commits `839e6b1` through `c3449b4` fail `cargo test --workspace` (the CLI's fake app-server test) until `baa4e50` fixed the test. The repository merges with merge commits, so they enter main's history. For the owner: a `git bisect` across that range must skip them; every later commit on the branch passes the workspace tests.

## Usage/release batch (2026-09-26, branch `codex/w3-usage-release`) — v0.1 limitations and decisions recorded here, not fixed

- **D13: no product budget enforcement.** PIO records usage and liability (`execution.view.usage`) but enforces no budget of its own; every stop so far (M2's Codex cap, M3's Claude cap, L1/L1b/L3's MiniMax cap and per-run ceilings) has been the **live runner's**, external to the product (`scripts/lead_run.py`, `scripts/*_live_run.py`), not something `execution.rs` reads and refuses on. No adapter service may advertise `enforced_bounds`; a test now asserts so (`crates/pio-protocol/src/stream.rs`, `no_adapter_service_may_declare_enforced_bounds`), so a future admission decision that starts claiming it would fail. This is a named v0.1 limitation, not scheduled work here; also listed in the README's "what is not established".
- **D14: Protocol #15, recovery after an acknowledged delivery.** [Protocol issue #15](https://github.com/Combraton/protocol/issues/15) (open, carried since M2) — recovery can still record `ambiguous` / `dispatch_may_have_begun` for an execution whose delivery a receipt already shows `acknowledged` (`m4/evidence/R2.json`, `j3.recovery`). PIO cannot resolve this locally: it is a Protocol-level recovery-decision gap, not a PIO defect, and stays open upstream. Tracked here as a v0.1 limitation, unchanged from M2's "Upstream" note above.
- **D17: the thread-settings guard's trusted-project default is not a defect.** See [ADR 003](../decisions/003-codex-app-server-adapter.md#d17-the-thread-settings-guards-trusted-project-default-is-not-a-defect-orchestrator-decision-2026-09-26): PIO only ever requests `workspace-write` explicitly, in its own throwaway fixtures, and the offline probe's `requested_workspace_write_on_request` case measured that Codex grants an explicit `workspace-write` request on an untrusted, unconfigured project regardless of the guard's trusted-project assumption. The guard only compares requested-versus-configured breadth; it never uses the assumed default to grant anything. Closed as a non-defect, orchestrator decision 2026-09-26.
