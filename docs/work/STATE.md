# Current session state — PIO

Dated snapshot; reconcile Git with [M1 issue #3](https://github.com/Combraton/pio/issues/3) and [draft PR #4](https://github.com/Combraton/pio/pull/4). Issues own live progress.

- **Updated:** 2026-09-16. **Active correction head:** `baf2faed1ce5cf2fee57bbb36e99973439561310`; base unchanged. Owner reproduced this checkpoint and requested [five bounded M1 acceptance corrections](m1/ACCEPTANCE-CORRECTIONS.md). Five corrections implemented; 19 tests, Clippy/build/docs, 22 public smoke cases and local launchd packaging checks pass. Full clean-clone and two-platform verification is next; these are development receipts. **Owner:** Codex, standalone PIO implementation lead.
- **Branch/base/head:** `codex/m1-core-host`, one implementation worktree; base `900bc03cfb0c898faa14f5e8b68afe4ac7ff26fe`. **Tested implementation head:** `0c1fb06e44e5b675fd749d65876fb9f60bdabb95`. This documentation-only successor records its receipts. [Evidence index](m1/evidence/public-host-checkpoint.json) records clean source, exact hashes, classes and both CI platforms. No sibling repository modified.
- **Owner verdict:** M0 accepted and merged as `900bc03`. Owner independently verified scripted Execution `7a4659f` (15 tests, matrix 54, runner 206/73/1, two separate supplemental fixtures and timeout mutant). ADR 002 and its implementation accepted. **M1 remains incomplete and unaccepted.**
- **Authority:** [PLAN](standalone-0.1/PLAN.md), [ADR 001](../decisions/001-standalone-stack.md), [ADR 002](../decisions/002-protocol-journal.md), issue #3. Shared architecture inspected at `9af69ce966bfacf0deb03606d99f28a355d1f944`; Protocol pin `cbf8e4df9df2ca8a9b50264df6acace6e4c3a0fc`.
- **Public process checkpoint:** `3fbfcee` connected Unix `execution.submit` to the journaled, launch-guarded, slot-locked durable fake-process host. Delivery follows child release markers; output/exit follow owned pipes and process receipts. Daemon restart re-observes the same kernel identity; lost hosts never respawn automatically. Public matrix uses authenticated Protocol operations, with independent kernel/journal witnesses. The diagnostic daemon remains test tooling. [Details and issue #3 mapping](m1/PUBLIC-HOST.md).
- **Output/caller persistence:** immutable SHA-256 spool objects are synced before digest/offset/lost-range references enter the journal. A separate bounded caller ledger writes exact command identity and selected basis before I/O; `client reconcile` queries pending operations and fetches the original acknowledgment by exact replay. Empty reconciliation stays pending. No copied mutable service store or stored credential bytes.
- **Verification corrections:** `c9d1774` scoped private-root checks to the durable service after the pinned runner exposed a startup regression. Linux CI then exposed observer opens rerunning schema writes; `0c1fb06` removes those writes for existing stores. A deterministic writer-lock test covers that failure, and host cut-point tests require their intended exit codes. Scoped Sol review findings on dead pre-release owners, spawn cleanup and evidence provenance were fixed and re-reviewed.
- **Exact-head evidence:** [CI 35091218825](https://github.com/Combraton/pio/actions/runs/35091218825) succeeds on macOS arm64 and Linux x86_64. A fresh HTTPS clone at the same head, fresh stores and freshly downloaded pinned release assets completes the documented sequence with exit 0. **19 Cargo tests**, fmt, build, Clippy, docs and lockfile checks pass. All 64 vendored schemas match the release inventory.
- **Runner:** **206 pass / 73 unsupported / 1 skipped**, zero fail/timeout/harness_error; **two supplemental passes remain separate**. Core 134 pass/1 unsupported, stream 24 pass, socket 12 pass/1 unsupported, Execution 36 pass/15 unsupported. All official fixture classes match between the clone and both CI artifacts. Detailed directory counts and unsupported reasons remain in the report/manifest.
- **Matrices and caller proof:** public matrix **54 attempted = 39 pass + 3 expected identity/count failures + 9 expected defense refusals + 3 expected wrong-reason classifier failures**. Diagnostic matrix independently matches those classes. Public transcripts validate against frozen result schemas. Caller/CAS proof passes on all three environments with exactly one child after lost-response recovery. Artifacts preserve identity, slot, generation, marker and process-table evidence.
- **Limits:** process mode has submit/inspect/reconcile/output/controller behavior; cancellation and workspace/usage adapter behavior remain explicitly unavailable. The five-feature conformance claim remains attached to the scripted participant. [Protocol #10](https://github.com/Combraton/protocol/issues/10) and the schema demonstration explain why detailed process witnesses remain outside frozen public result fields. Protocol #9 and the undeclared subscription recheck barrier remain coverage limits. Commit still diffs the whole cache; ADR 002 names the pre-M2 limit (32 MiB / 32,768 records), not yet enforced. No total spool-GC/disk or production-throughput claim.
- **Carry-forward:** M2 canonical-JSON drift checks and Codex trusted-project config side effect; M3 distinguishing API/provider credential-source evidence and refusal without configured credentials despite cached login. No real harness, native authentication or end-to-end journey validated. **All six product journeys remain `not_evaluated`.**
- **Resources/next:** no active helper or retained fake process from completed test cases. Read-only MiniMax M3 inventory completed (13,798 tokens, $0.004564); scoped Sol review completed. Temporary clean clones/artifacts remain outside Git. Next is full verification of the bounded corrections, then owner M1 acceptance review. PR stays draft; no M1 merge, tag or publication authorization. User source attachment preserved.
