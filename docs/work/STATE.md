# Current session state — PIO

Dated snapshot; reconcile Git with [M1 issue #3](https://github.com/Combraton/pio/issues/3) and [draft PR #4](https://github.com/Combraton/pio/pull/4). Issues own live progress.

- **Updated:** 2026-09-16. **Owner:** Codex, standalone PIO implementation lead.
- **M0:** accepted follow-up `4549c22`; PR #2 merged with authorization as `900bc03cfb0c898faa14f5e8b68afe4ac7ff26fe`.
- **Branch/base:** `codex/m1-core-host`, one implementation worktree; base `900bc03cfb0c898faa14f5e8b68afe4ac7ff26fe`. **Tested behavior HEAD:** `eff952ddf875d506cb000f16f8ddcd1f82f099b7`; this evidence-only successor records that implementation checkpoint. Artifact environment receipts record the actual tested head and dirty state. No sibling repository changed.
- **Authority:** [PLAN](standalone-0.1/PLAN.md), [ADR](../decisions/001-standalone-stack.md), issue #3 acceptance. Shared architecture inspected at Combraton `9af69ce966bfacf0deb03606d99f28a355d1f944`; Protocol source pin `cbf8e4df9df2ca8a9b50264df6acace6e4c3a0fc`.
- **Execution design checkpoint:** inspected head `fa70a630c31c8f207ecb449cba69d52111242fcb`, base `900bc03cfb0c898faa14f5e8b68afe4ac7ff26fe`. Owner verified Core; M1 remains incomplete. [ADR 002](../decisions/002-protocol-journal.md) selects Core migration to the shared journal before any Execution code. Descriptor renamed `pio-core-conformance`; service loop explicitly conformance-only. No Execution code written at this checkpoint.
- **Historical owner verdict:** lower-level checkpoint `4bc8f37` accepted for continuation, not M1 acceptance. Corrections committed as `38ca384`. Its CI caught a transient `.pending` evidence-file collection race, fixed in this successor without relaxing the behavior properties.
- **Current implementation:** conformance-only Unix stream/authentication/negotiation, core-test, grants, durable events/subscriptions and capabilities. SQLite commits state, bound outcomes and events atomically. Test-only effect-store groundwork exists but public effects remain undeclared until pinned Execution fixtures run. [Core checkpoint](m1/CORE.md) documents boundaries and exact coverage.
- **Local checks:** format/build, nine Cargo unit tests, Clippy with warnings denied, docs checks and pinned runner. Runner: **164 pass, 115 unsupported, 1 skipped, zero fail/timeout/harness_error**. Core 130/135, stream 24/24, socket 10/13 pass. Eight Core/stream/socket coverage limits remain explicit. Local artifacts: `target/core-checkpoint`.
- **Fake-host evidence:** 54 attempted cases, three repetitions: 39 passes, 3 intended J3 count failures, 9 exact defense refusals, 3 intended wrong-reason classifier failures. Includes same-child restart/reattach, fenced known-not-released observation and actual read-only-store no-spawn proof. Artifacts: `target/host-core-final`. [Historical checkpoints](m1/CHECKPOINTS.md) preserve prior heads and CI evidence.
- **Claims/controls:** core/1 and core-test/1; core.grants/events/capabilities; clock.file/store.faults. Execution, core.effects, core.events.backpressure, executor.script and all barriers unclaimed. `pio-report.json` includes native classes by directory and every unsupported/skipped reason. Vendored schemas must match the verified archive byte for byte.
- **Remaining M1:** Execution subset/scripted executor and public effect lifecycle, backpressure and missing controls, caller operation persistence, content-addressed payloads, full process certification. **M1 is incomplete and unaccepted.** No real adapter, CBR service, native credential or native configuration was used.
- **Carry-forward:** M2 canonical JSON schema drift checks; Codex trusted-project config side effect; M3 distinguishing API/provider credential-source evidence and refusal with no configured credential despite an existing cached login.
- **Resources:** read-only fixture audit completed; the broader review and one narrowed retry were stopped without a final report and supply no review verdict. No helper or concurrent writer remains. Matrix processes are cleaned up; diagnostic stores and review logs remain outside Git. Both-platform CI and a fresh clone with fresh Protocol downloads passed at `eff952d`; [receipt index](m1/evidence/core-checkpoint.json). Negotiation edge-case regression tests also pass in CI. The socket whitespace regression also passes on both platforms.
- **Next:** implement and verify the ADR 002 journal migration, then the requested Execution sequence. Historical handoff: Core checkpoint is ready for review in draft PR #4; implement the PLAN Execution subset and scripted executor in the next behavior checkpoint. Execution/scripted executor belong to the following checkpoint. No M1 merge, tag or publication authorization. User source attachment preserved.
