# Current session state — PIO

Dated snapshot; reconcile Git with [M1 issue #3](https://github.com/Combraton/pio/issues/3) and [draft PR #4](https://github.com/Combraton/pio/pull/4). Issues own live progress.

- **Updated:** 2026-09-16. **Owner:** Codex, standalone PIO implementation lead.
- **Branch/base:** `codex/m1-core-host`, one implementation worktree; base `900bc03cfb0c898faa14f5e8b68afe4ac7ff26fe`. **Tested implementation head:** `245856a43426422b4271f32156a9e5a08c489ba9`; this evidence-only successor records the completed Execution checkpoint. [Receipt index](m1/evidence/execution-checkpoint.json) records actual tested HEAD, clean state and source/binary hashes. No sibling repository modified.
- **Owner verdict:** M0 accepted and merged as `900bc03`; Core checkpoint `fa70a63` independently verified (9 tests, matrix 54, pinned runner 164/115/1, replay and authorization mutants). M1 remains incomplete and unaccepted.
- **Authority:** [PLAN](standalone-0.1/PLAN.md), [ADR 001](../decisions/001-standalone-stack.md), [ADR 002](../decisions/002-protocol-journal.md), issue #3. Shared architecture inspected at `9af69ce966bfacf0deb03606d99f28a355d1f944`; Protocol pin `cbf8e4df9df2ca8a9b50264df6acace6e4c3a0fc`.
- **Design before behavior:** `c6b5b8f` recorded the choice to move Core onto the journal before Execution. `5f2e386` implemented per-record journal/projection/outbox transactions and rollback reload; 10 tests and unchanged runner 164/115/1 verified. Legacy blob stores are explicitly refused. Provider memory remains a full read cache and persistence scans retained records; no throughput/total-memory claim.
- **Execution checkpoint:** `4a0ec44` implements `executor.script` on the labeled fake host, the five PLAN features and public effects. Execution directory: **36 pass / 15 unsupported**, zero failures; full subset run **201 pass / 78 unsupported / 1 skipped**. [Execution details](m1/EXECUTION.md). Fifteen Cargo tests and Clippy now pass, including atomic admission/replay/rebuild and a draining output consumer.
- **Backpressure checkpoint:** four frozen slow-consumer cases pass locally, including one shared ending-notice budget; socket **12 pass / 1 unsupported**. Full suite at `245856a`: **206 pass / 73 unsupported / 1 skipped**, plus **2 separately counted supplemental passes**. A clean independent checkout and CI run `35086098791` match every official outcome on macOS arm64 and Linux x86_64. Core 134/135, stream 24/24, socket 12/13, Execution 36/51 pass. Fifteen unit tests and the matrix 54 with all four expected classes pass. Claims now include `core.events.backpressure` and its three signals plus `session.closed`; the polling provider remains a conformance service loop, not the durable-host design.
- **Coverage limits:** six of the seven requested Execution-dependent Core/socket cases can run in scope. `core.feature-dependencies-match-negotiation` also requires four other product profiles and `execution.context`, and remains unsupported. [Protocol #9](https://github.com/Combraton/protocol/issues/9) includes a demonstrating Execution-only fixture; the same pinned runner passes it in a separate PIO regression manifest. The deliberately paused subscription authorization-race barrier remains unsupported. Never add supplemental passes to the official 280-fixture count.
- **Remaining M1:** durable caller operations, content-addressed payloads, integration of public Execution with surviving native process ownership, full process/storage certification. The scripted adapter models host observations; the existing lower-level process matrix separately proves its identity/count properties. Neither is a real adapter or end-to-end M1 acceptance.
- **Carry-forward:** M2 canonical-JSON drift checks and Codex trusted-project config side effect; M3 distinguishing API/provider credential-source evidence and refusal without configured credentials despite cached login.
- **Resources:** read-only MiniMax M3 contract audit completed, exit 0, 28,170 tokens, $0.012545; the bounded code-review helper was stopped without a final report. One narrow retry confirmed only shared notice-deadline placement (5,070 tokens, $0.001124), not a full review. No helper remains. No native harness credential/configuration used. Temporary conformance artifacts remain outside Git.
- **Self-review:** cancellation forwarding pins its own payload digest and idempotency key before attempting; timeout observations do not postpone inactivity; valid-generation dispatch and later crash preserve one attempt; unsupported adapter controls refuse startup.
- **Active public-host checkpoint:** starting head `7a4659f423737a5341e198dd779392e54b797458`, base unchanged; owner accepted the scripted checkpoint and ADR 002. [Public-host record](m1/PUBLIC-HOST.md) tracks the implementation and issue #3 mapping. Public `serve-fake` now calls the durable process host, output is content-addressed, and a distinct caller ledger reconciles pending operations. First 18-case public matrix and caller/CAS proof pass; 18 Cargo tests and Clippy pass. Scoped review corrections are implemented; exact-head full matrices, runner, clean clone and both CI platforms are next. Protocol #10 documents the process-witness schema gap without widening schemas.
- **Verification correction:** implementation head `3fbfcee196aaa633515ea41a0bbb77a83254210d` passed both process matrices but its full runner caught a conformance-directory permission regression (204 startup failures). The private-root check is now scoped to `serve-fake`; unchanged runner-created conformance directories retain the prior contract. Reverify at the correction head.
- **Linux verification correction:** `c9d1774` restored local runner 206/73/1 plus two supplemental passes. Linux CI found host-start contention because every Store open reran migration writes. Existing version-2 stores now open without DDL/meta writes; the three host cut-point cases also assert their intended exit codes, preventing an unrelated failure from satisfying the property. Exact-head rerun required.
- **Current resources/next:** one implementation writer, no active helpers. Source and docs changes are the implementation checkpoint in this commit; verified baseline remains `7a4659f` until new exact-head receipts are recorded. No real adapter, native authentication or end-to-end claim; all six journeys remain `not_evaluated`. No M1 merge/tag/release authorization.
